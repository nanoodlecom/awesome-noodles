# Gallery curation — 2026-09-05

The gallery retains 38 distinct jobs and archives two experiments. The bar is a reusable task with meaningful inputs, an appropriate model and an inspectable output. A different fictional setting does not earn a separate card.

## Decisions and acceptance checks

All graph paths below are relative to `graphs/` and end in `.noodle-graph.json`.

| Graph | Decision and purpose | Model choice and cost basis | Accept the output only when |
| --- | --- | --- | --- |
| `character-sprites` | Keep the existing worked example: character art and cutout parts feed a local rig and playable game. Existing graph and artifacts preserved. | GLM Flash structures the character brief; Muse text-to-image and edit preserve a common reference. Two image calls at $0.01 each, plus text. | Character details agree between reference and parts; body, arm and two legs are separable. Local rig frames need joint/motion review. The graph alone never promises an atlas or game. |
| `edit-a-photo` | Improve: clean a product photo for a catalog, replacing an unrelated neon-scene transformation. First-click Edit request matches nanoodle's cool first-click vibe pass: cool night-ride catalog (matte charcoal seamless, hard cyan rim, subtle contact shadow), not the warm-white seamless / soft even studio light. | Muse Edit supports prompt-guided reference editing; $0.01/image. Preserve input aspect with `auto`. | Product shape, color, material, marks and text survive; no invented accessories; clean background and plausible contact shadow. Test a different product. |
| `combine-images` | Improve: preview a product in a supplied setting, replacing a fantasy portrait composite. First-click Placement brief matches nanoodle's cool first-click vibe pass: Volt pocket night-ride radio (matte charcoal body, cyan lightning chevron) in a wet night alley / neon wet-asphalt courier lane, not generic photoshoot-placement language. | Muse Edit accepts multiple reference images; $0.01/image. | Both references matter; product identity and setting layout survive; scale, perspective and shadow are plausible. |
| `image-model-arena` | Keep and improve: a shared poster brief gives a concrete model-selection task. First-click Shared test brief matches nanoodle's cool first-click vibe pass: Volt night-ride poster (exact heading NIGHT RIDE, exact footer TUE 9 SEP, one matte-charcoal pocket radio with one electric-cyan lightning-bolt chevron on dark slate), not the cream FIX A FLAT bicycle-repair workshop. | Muse, GPT Image 2.5 Flare, Grok Imagine and Recraft provide four distinct image models. Four paid calls, no automatic winner claim. Contender 2 pins live `openai/gpt-image-2.5/flare/text-to-image` at `1k` ($0.153 catalog 2026-09-09), replacing `krea-v2/turbo`. Sunburst is not this swap. No paid regen this pass. | Compare exact heading/footer (NIGHT RIDE / TUE 9 SEP), one pocket radio, one lightning-bolt chevron, charcoal/cyan palette and composition. All contestants see the same prompt. Record failures instead of choosing by visual polish alone. |
| `render-a-mockup` | Improve: screen brief with supplied labels and values; replace generic premium-dashboard filler. Remove the LLM after a live run exceeded Qwen’s 800-character prompt limit; direct brief and style total 696 characters. First-click Screen brief/Visual style matches nanoodle's cool first-click vibe pass: Volt courier dispatch (charcoal background, electric-cyan accents), not the repair-shop / forest-green dashboard. | Qwen Image 3 Pro renders the supplied brief and style directly at 1K ($0.04/image), avoiding needless prompt expansion. | Requested rows, labels, names and values remain legible; requested charcoal/cyan style is respected; no unrelated charts or invented metrics. The output is an image. |
| `favicon` | Improve: retain brand-to-glyph task; remove duplicated style nodes and join. First-click Brand matches nanoodle's cool first-click vibe pass: Volt pocket night-ride radio for couriers (sharp lightning-bolt chevron, matte charcoal, electric-cyan), not the Lumen weather-radio lighthouse navy/gold. | One bounded GLM Flash call condenses the brand; Muse renders a square concept ($0.01/image). | One bold glyph, requested palette (charcoal field, electric-cyan lightning chevron), clear margins and recognizable silhouette at 16px. No text, mockup or tiny detail. Conversion into actual favicon sizes remains separate. |
| `night-market-postcard` | Improve: reusable travel illustration, with sample destination kept only in the input. | GLM Flash combines destination and mood; Muse supports 3:2 ($0.01/image). | Destination-specific details match the brief, the selected style appears, and an alternate place does not inherit squid, lanterns or wet asphalt. |
| `fibo-studio-still` | Improve: a described-product editorial concept, distinct from reference-preserving photo editing. Remove mandatory brass/slate/fog from system prompt. First-click Product/Light matches nanoodle's cool first-click vibe pass: Volt pocket night-ride radio (matte charcoal, electric-cyan lightning-bolt chevron, hard cyan rim light on dark slate), not the amber glass hand-soap bottle. | GLM Flash writes structured descriptive prose; FIBO provides photoreal product styling at 1MP ($0.04/image). Provider licensed-data positioning is not a legal-clearance guarantee. | Product count, material, color and selected light match the brief; no invented packaging, logos or props. A second product must not inherit the first product's materials. |
| `photo-to-video` | Improve: a product still plus a small motion; remove redundant motion-writing LLM and empty LoRA row. Fix invalid Muse `1k` size to supported `16:9`. First-click Still/Motion matches nanoodle's cool first-click vibe pass: Volt pocket night-ride radio on wet asphalt (cyan edge light, night-city bokeh, locked-camera rain streaks), not the cozy tea-mug morning desk. Distinct from Omni Flash studio-plinth Volt radio turntable (orbit / hard cyan rim). Distinct from `grok-imagine-still` (Grok Imagine Video 1.5 rooftop-ledge still + one cyan pulse). Distinct from `wan-still-audio` (Wan 3.0 railing + synced audio, 2s). | Muse creates the first frame ($0.01); MiniMax H3 Spicy accepts image-to-video, 480p/5s ($0.20). Task uses ordinary product imagery; model choice is for this capability and bounded price. | The radio remains stable on the wet asphalt, rain streaks move coherently, and there are no cuts or extra objects. Seamless looping is not promised. |
| `grok-imagine-still` | Keep: still → Grok Imagine Video 1.5 image-to-video. Card title **frame wakes up**; desc **one still — Grok 1.5 rides it into motion**. First-click Still/Motion is Volt charcoal/cyan radio on a wet rooftop concrete ledge — locked cam, subtle rain beads, one cyan chevron pulse. Distinct from live `photo-to-video` (MiniMax H3 Spicy wet-asphalt rain loop, 5s). Distinct from `wan-still-audio` (Wan 3.0 railing + synced audio, 2s). Do not retarget those graphs. Do not pin older `grok-imagine-video`, Wan, or Seedance. Working nodes are text Still brief → Muse 16:9 still → ivideo (motion brief wired to prompt). Custom-endpoint stays retired. No paid QC; no fake paid thumb. | Live video catalog 2026-09-09: `xai/grok-imagine-video/v1.5/image-to-video` (Grok Imagine Video 1.5; not v1 `grok-imagine-video`). Image-to-video; tags xai / grok / image-to-video. Pins `resolution=480p`, `duration=4` (4s is a listed option; catalog default is 6s / 720p). Image-mode quote: $0.204/s at 480p + $0.017 `image_input_fee` → 4 × $0.204 + $0.017 = $0.833 video; Muse still $0.01. Text-mode and 720p rates are higher/different and are not this pin. | Radio identity and cyan chevron survive; locked camera; subtle rain; one cyan pulse; no cuts or extra objects. Changing still or motion brief must change the clip. Sample-gallery cover QC is pending — do not invent a paid thumb. |
| `wan-still-audio` | Keep: still → Wan 3.0 image-to-video with synced audio. Card title **still breathes with sound**; desc **one still — Wan 3.0 adds motion + synced audio**. First-click Still/Motion is Volt charcoal/cyan radio on a rain-slick steel railing above a neon alley — magenta rim left, cyan puddle bounce, locked-cam breath (rain beads crawl, puddle shimmer, one soft LED pulse, faint alley hum in generated audio). Distinct from `grok-imagine-still` (Grok Imagine Video 1.5 rooftop pulse, 4s, no Wan audio) and from `photo-to-video` (MiniMax H3 Spicy wet-asphalt rain, 5s). Do not retarget those graphs. Distinct from Omni Flash turntable, H3 Max multi-angle, Crystal upscale, InfiniteTalk, and any Seedance pin. Distinct from `wan-motion-drive` (still + driver clip, Animate 2 motion transfer). Do not retarget that graph. Working nodes are text Still brief → Muse 16:9 still → ivideo (motion brief wired to prompt). Custom-endpoint stays retired. No paid QC; cover honesty deferred. | Live video catalog 2026-09-09: `alibaba/wan-3.0/image-to-video` (Wan 3.0 Image-to-Video; not Prime, not text-to-video, not reference-to-video, not Wan 2.7). Image-to-video; tags wan / alibaba / image-to-video / audio. Pins `resolution=480p`, `duration=2` (catalog type is number, not a select — store `2` not `"2"` so the editor does not fall back to default 5), `enable_audio=true`, `thinking_mode=false` (`enable_audio` / `thinking_mode` / `duration` in `modelOpts` so the runner forwards them). Catalog $0.07/s at 480p → 2 × $0.07 = $0.14 video; Muse still $0.01. Catalog default is 5s / 720p ($0.13/s ≈ $0.65) — do not pin that. Duration 2 is the catalog minimum (2–30s). | Radio identity and cyan chevron survive; locked camera; rain beads / puddle shimmer / one LED pulse; generated audio carries a faint alley hum; no whip-pans, cuts or extra objects. Changing still or motion brief must change the clip. Sample-gallery cover QC is pending — do not invent a paid thumb. |
| `wan-motion-drive` | Keep: still + driver clip → Wan 2.2 Animate 2 motion transfer. Card title **drive the still with motion**; desc **one still + a driver clip — Wan Animate 2 transfers the move**. First-click is two upload placeholders: Character/product still (Volt charcoal/cyan radio or courier) + Driver motion (simple hand-wave / turn, ~2–3s). Optional Motion prompt keeps the radio/chevron and transfers only the driver move. Distinct from prompt-i2v: `photo-to-video`, `grok-imagine-still`, `wan-still-audio` (Wan 3.0 i2v + synced audio). Distinct from InfiniteTalk / talking-avatar (lipsync), H3 Max multi-angle, and `p-video-rewrite`. Do not retarget those graphs. Working nodes are upload still + vupload driver + text Motion prompt → vedit (comments do not count). Still is wired to vedit `image` and `ref1`; driver to `video`; prompt to `prompt`. Custom-endpoint stays retired. No paid QC; no fake paid thumb. | Live video catalog 2026-09-09: `wan-22-animate-2` (Wan 2.2 Animate 2; not `wan-22-animate` v1 at $0.05/s). Image+video→video; tags wan / animate / motion-control. Pins `resolution=480p` (catalog default; 720p is $0.08/s). Catalog $0.04/s at 480p → ~2–3s driver ≈ $0.08–0.12. No duration field — billing follows the driver clip. Optional catalog `motion_prompt` rides as the wired vedit prompt. Do not swap to `pruna-ai/p-video/animate`. | Radio/courier identity and cyan chevron survive; driver motion transfers; no new objects, logo text, or hard cuts. Changing the still, driver, or Motion prompt must change the clip. Sample-gallery cover QC is pending — do not invent a paid thumb. |
| `omni-flash-turntable` | Improve: product motion concept with one selected move, no hardcoded harbor scene. Lower default to 5s/360p. First-click Object/Move matches nanoodle's cool first-click vibe pass: Volt pocket night-ride radio on a charcoal stone plinth (hard cyan rim, electric-cyan lightning-bolt chevron, slow quarter-orbit), not the chrome motorcycle helmet. Distinct from photo-to-video rain-slick asphalt + locked-camera rain beads — this is studio plinth + orbit/rim light. | GLM Flash combines object and move; Omni Flash 1.1 supports text-to-video at these settings ($0.195 video step). | Selected camera/object motion occurs once, geometry stays stable, object count and colors match (matte-charcoal Volt radio, cyan lightning chevron, hard cyan rim). This generates a concept object, not an exact product replica. |
| `talking-avatar` | Improve: useful short spoken product intro with a clear fictional presenter, uncluttered portrait and editable script. First-click Presenter look/Spoken script matches nanoodle's cool first-click vibe pass: Volt night-courier dispatcher (charcoal kit, electric-cyan lightning chevron, magenta rim + cyan bounce), not the grease-stained bike-shop mechanic / kettle script. Distinct from cinematic-character-still helmet-under-arm alley hero key-art; this stays a talking-head portrait for LongCat lipsync. Distinct from `night-ride-radio-vo` (speech-only text→TTS, no portrait/lipsync). | Muse portrait + MiniMax Speech 2.8 HD + LongCat image/audio lipsync. LongCat 480p costs $0.03 per audio second; image and TTS extra. | Spoken text and pronunciation are correct, face remains visible, mouth timing follows speech, and script stays below the 30-second model limit. |
| `infinitetalk-radio-take` | Keep: still + driving audio → InfiniteTalk speaking/singing courier clip (lips + body follow the sound). Card title **Night-ride radio take**. First-click Still brief is a mid-shot Volt night courier on a rain-slick rooftop ledge (matte charcoal jacket, one electric-cyan lightning-chevron, pocket radio held chest-high, magenta rim + cyan bounce) — not the talking-avatar shoulders-up spoken product intro. Speech bootstrap is first-click only so Run has audio without an upload asset yet; prefer swapping it for an Audio upload of a real radio-style take. No LongCat. Do not retarget `talking-avatar`. Distinct from `night-ride-radio-vo` (speech-only text→TTS, no still/video). | Live video catalog 2026-09-09: `infinitetalk` (all lowercase; no slash prefix). Image+audio→video; tags video/avatar. Pins `people=single`, `resolution=480p` (`people` in `modelOpts` so the runner forwards it). Catalog minimum ~$0.09 (duration/resolution); Muse ~$0.01; Speech only if the bootstrap stays. No paid QC in this first PR. | Face and radio hand stay identity-stable; cyan chevron readable; lips and torso follow the take; locked camera; one person; no cuts. Audio under ~15 s for a cheap first-click. This model follows the sound; it does not invent a TTS script. A later reviewed Run should capture duration, quoted $, and body-motion pass/fail. |
| `sing` | Improve: original closing-credits song; remove two style/negative-prompt LLMs and artist-name input. First-click brief/style match nanoodle EXAMPLES after #478: rooftop getaway night-city chase + cinematic synthwave, not a cozy last-stall folk scene. | One GLM Flash call writes bounded lyrics; Mureka Generate Song consumes supplied lyrics and style ($0.225 music step). Catalog does not justify a separate negative-prompt writer. | Lyrics are original, intelligible, fit the rooftop-getaway closing-credits brief and supplied cinematic-synthwave style, and have a usable chorus. Precise duration/section execution is not guaranteed. |
| `night-ride-sfx` | Keep: text brief → short Volt radio / cyan-thunder sound effect. Card title **Night-ride SFX**. Ultra-cheap first-click, distinct from Closing-credits song (`sing` / Mureka generate-song), Night-ride radio VO (`night-ride-radio-vo` / SpaceXAI TTS `xai-tts`), and video→foley (`mirelo-video-foley` / `mirelo-ai/sfx1.6/video-to-video`). First-click SFX brief is a Volt night-ride radio sting: wet neon alley pulse, low radio static bloom into one electric-cyan thunder hit, then tight decay; pocket-radio energy, ~4s, no music bed, no vocals, no dialogue. Working nodes are text SFX brief → music Sting (comments do not count). No upload. No LLM. No image/video. Custom-endpoint stays retired. Do not retarget `sing` or any TTS graph. | Live audio catalog 2026-09-09: `elevenlabs/sound-effects/v2` (ElevenLabs Sound Effects V2; no prefix strip). Text→audio; tags audio_generation. Pin `duration=4` on the music node (runner already forwards duration; catalog range 0.5–22s). Catalog $0.002/s → ~$0.008 at 4s. `response_format=mp3`. Not `elevenlabs/music` and not ElevenLabs TTS (`Elevenlabs-Turbo-V2.5` / `Elevenlabs-V3`). No paid QC. | Sting is a short SFX (~4s), not a song, not speech, not a music bed. Radio-static bloom then one electric-cyan thunder hit with tight decay; no vocals or dialogue. Changing SFX brief must change the sting. Cover QC pending (`graphs/night-ride-sfx.NOTE.txt`). |
| `whisper-pull-words` | Keep: upload VO → Whisper Large V3 speech→text transcript. Card title **pull the words off the take**; desc **drop a night-ride VO — Whisper prints the transcript**. First-click is an audio-upload placeholder: Volt charcoal/cyan radio night-channel take (`graphs/whisper-pull-words.NOTE.txt`). Working nodes are aupload Night-ride VO → transcribe Transcript (comments do not count). Thin first-click: upload VO + run. Speech→text — opposite of Night-ride radio VO (`night-ride-radio-vo` / `xai-tts` text→speech). Distinct from InfiniteTalk / talking-avatar (lipsync) and Night-ride SFX / Mirelo foley (sound beds). Custom-endpoint stays retired. Use the `transcribe` node (not llm-with-audio). No Demucs / Sonilo / stem. Do not pin `gpt-4o-mini-transcribe*`, `whisper-1`, or ElevenLabs Scribe. No paid QC — aupload placeholder only; no fake transcript thumb. | Live audio catalog 2026-09-09 (rechecked live 2026-09-10): `Whisper-Large-V3` (Whisper Large V3; no prefix strip). Audio→text; category `audio_stt`; `capabilities.speech_to_text`. Catalog $0.000495/min (≪$0.01 for short VO). Language auto/blank. Do not pin `gpt-4o-mini-transcribe*`, `whisper-1`, or `Elevenlabs-STT` / Scribe (diarization deferred). | Transcript is speech→text of the uploaded VO, not a TTS line, not a lipsync clip, not an SFX bed. Changing the uploaded take must change the transcript. Cover QC pending — do not invent a transcript thumb. |
| `night-ride-radio-vo` | Keep: spoken script → speech-only dispatch callout VO. Card title **Night-ride radio VO**. Ultra-cheap dedicated TTS first-click, distinct from Spoken introduction (`talking-avatar`: Muse portrait + MiniMax Speech 2.8 HD + LongCat lipsync video), Night-ride radio take (`infinitetalk-radio-take`: still + audio → InfiniteTalk video), Night-ride SFX (`night-ride-sfx` / `elevenlabs/sound-effects/v2` music-node sting), video→foley (`mirelo-video-foley` / `mirelo-ai/sfx1.6/video-to-video`), Closing-credits song (`sing` / Mureka generate-song), and pull the words off the take (`whisper-pull-words` / `Whisper-Large-V3` speech→text). First-click Spoken script is a cool Volt night-ride dispatch callout — night board, pocket radio, cyan channel — not a cozy workshop. Working nodes are text Spoken script → tts Speech (comments do not count). No image, no video, no LLM, no upload. Custom-endpoint stays retired. Do not retarget `talking-avatar`, `infinitetalk-radio-take`, `night-ride-sfx`, or `sing`. Do not pin MiniMax Speech, ElevenLabs TTS, Kokoro, or Omnivoice for this graph. | Live audio catalog 2026-09-10: `xai-tts` (SpaceXAI TTS). Resolves as-is (no prefix strip). Modality text→speech (`capabilities.text_to_speech`); category `audio_tts`. Voices Eve / Ara / Leo / Rex / Sal — pin `voice=Leo` (calm male dispatch energy; matches prior Volt dispatcher VO gender). TTS node forwards `voice` when the catalog lists voices. Catalog $0.0165 per 1k chars; first-click script is 138 chars (~$0.0023, well under $0.01). `max_chars` 5000. `response_format=mp3` (default OK). No Delivery / `instructions` field — graph stays comment + Spoken script → Speech. No paid QC. | Output is speech of the Spoken script (Leo voice), not a song, not an SFX sting, not a lipsync video. Changing Spoken script must change the VO. Cover QC pending (`graphs/night-ride-radio-vo.NOTE.txt`); do not invent a reviewed mp3. |
| `deslop` | Improve: midnight drop notice — keep $280 & 00:01, lose the seamless unlock. Replace purported AI detector/confidence with an editorial comparison that sees both original and rewrite. Remove automatic sentence-cutting rule and inherited x402 budget. First-click seed matches nanoodle EXAMPLES after #477: midnight streetwear night-raid jacket drop (120 jackets, $280, 00:01 JST, Shibuya, drop@example.com), not the bicycle repair booking. First rewrite is now `venice-uncensored` uncensored burstiness (short punches + one longer clause); Terra review and Grok apply stay. Three-pass structure unchanged — not a two-node text slop graph. | First rewrite: `venice-uncensored` (uncensored burstiness / perplexity; live catalog 2026-09-09, $0.40/$1.80 per 1M). Replaces retired `openai/gpt-4o-mini` (too old). Terra independently checks source preservation. Grok applies the review. Three text calls earn their place through factual comparison, not invented authorship certainty. Free ZeroGPT 2026-09-09 on the committed CLEAN_v10 notice: slop draft ~20.9% AI → cleaned 0% AI (Human written). Same burstiness prompt: qwen3.6 7.1% AI; gemma-4 44.6% AI. GPTZero pending captcha. Do not invent other detector scores. | Every date, price, time, contact address and condition survives (120 jackets, $280, 00:01 JST, Shibuya studio, drop@example.com, 24-hour cancel); filler disappears; no invented claims. Reviewer flags actual discrepancies, never authorship probabilities. Committed sample is the CLEAN_v10 notice in `graphs/deslop.NOTE.txt`. |
| `fable-five-step` | Keep/Improve: distinct job (messy dump → shaped plan). Restored after the slop yank (#34 / nanoodle #459) with Choice+join so it is no longer two-node text. First-click keeps the Next.js invoices crime-scene seed (CSV export of page HTML, client-only date filter, localStorage, empty "no data", double-click export). Do not rewrite billing; custom-endpoints is a different ticket. | Model `anthropic/claude-fable-5.1`, maxTokens 512, reasoningEffort `low`, one text call (~$0.02). Choice selects the output shape; join prepends `=== OUTPUT SHAPE ===` before the prompt. | Shape matches Choice (five numbered steps, a GitHub PR body, or a failing-test sketch); concrete file/route names from the dump (`page.tsx`, `/invoices/export`, useState, localStorage); no billing rewrite; under ~180 words. |
| `cinematic-character-still` | Keep: photoreal cinematic character still from a short editable person brief, lighting and frame. Distinct from the travel postcard, FIBO product still, arena poster, talking-avatar presenter and sprite sheet. MiniMax H3 Image Edit (`minimax-h3/image-edit`) was considered for an identity-preserving restyle in the same job; it needs a reference upload and would not earn a second paid node beside a text-to-image still. That restyle job is now the separate Keep graph `h3-identity-restyle`. | Live image catalog 2026-09-08: `minimax-h3/text-to-image` (no `wavespeed-ai/` prefix). Photoreal/cinematic text-to-image; resolutions `1k`/`2k` at $0.02/$0.06. Default `1k`. Catalog also lists fifteen aspect ratios, but nanoodle-js only forwards `fields.aspect` for FIBO, so framing is an editable prompt choice rather than an unsent aspect field. No LLM. | Person, wardrobe, age, helmet, selected light and selected frame match the brief; one person; unobstructed face and mouth; no readable text or invented extras. Changing person, light or frame must change the still. Direction QC ($0.02) approved a motorcycle-courier night-alley still; committed sample gallery assets remain a follow-up. |
| `product-cutout` | Keep: dedicated product-still cutout for compositing. Distinct from Clean product photo (Muse Edit relights/replaces the backdrop), from SAM 3 text-selected isolate (`sam3-isolate` / `sam3-image`), and from Pixelcut video cutout (`pixelcut-video-cutout` / `pixelcut/video-background-removal`). First-click story matches the cool pass: Volt pocket night-ride radio (matte charcoal body, electric-cyan lightning-bolt chevron, dark slate / hard cyan rim). No committed still in-repo; the upload is the placeholder (`graphs/product-cutout.NOTE.txt`). Working nodes are upload → BiRefNet edit → local 1024 export resize (comments do not count). Custom-endpoint stays retired. | Live image catalog 2026-09-09: `birefnet/v2` (no `fal-ai/` prefix; marketing `/api/models` still lists `fal-ai/birefnet/v2`). Image→image only; size `auto` at $0.01. Marketing additionalParams default `mask_only: false`, `refine_foreground: true` — not advertised as Choice controls because nanoodle-js does not forward them. SAM 3 is a separate graph (`sam3-isolate`); not used here. The edit node pins a runner instruction because nanoodle-js refuses a blank edit prompt except for upscalers; catalog `input_modalities` are image-only, so that instruction is not a creative control. No LLM. Local resize is $0. | Product silhouette, materials, marks and text survive; fine edges stay; background gone (transparency, not a new seamless); no invented props. Changing the uploaded still must change the cutout. Inspect the native BiRefNet node for edges; Export preview is a local 1024 fit only. Sample-gallery cover QC is pending. |
| `sam3-isolate` | Keep: text-selected isolate of a named object or region from a cluttered scene. Distinct from Product cutout (BiRefNet V2 knocks out the whole background), from Clean product photo (Muse Edit relights/replaces the backdrop), and from Pixelcut video cutout (`pixelcut-video-cutout`). First-click Isolate names the matte-charcoal Volt pocket night-ride radio with one electric-cyan lightning-bolt chevron so first-click teaches text-selected segmentation. Scene is a cluttered courier desk or wet neon alley still that contains that radio — no committed still in-repo; the upload is the placeholder (`graphs/sam3-isolate.NOTE.txt`). Working nodes are upload → text Isolate → SAM 3 edit → local 1024 export resize (comments do not count). Custom-endpoint stays retired. | Live image catalog 2026-09-09: `sam3-image` (no `wavespeed-ai/` prefix; marketing `/api/models` still lists `wavespeed-ai/sam3-image`). Text+image; size `auto` at $0.005. Marketing additionalParams default `apply_mask: true` — left at catalog default; not advertised as a Choice because the editor does not require an explicit field. Distinct from `birefnet/v2`. No LLM. Local resize is $0. No paid QC. | Named object matches the Isolate text (radio + cyan chevron); rest of the cluttered scene is gone; no invented props. Changing Isolate text or the uploaded scene must change the isolate. Inspect the native SAM 3 node; Export preview is a local 1024 fit only. Sample-gallery cover QC is pending. |
| `h3-max-multi-angle` | Keep: product-reveal camera orbit from an uploaded still. Card title **Night-ride radio orbit**. First-click is a product-photo upload placeholder: matte-charcoal Volt pocket night-ride radio with one electric-cyan lightning-bolt chevron on dark slate / hard cyan rim (`graphs/h3-max-multi-angle.NOTE.txt`). Working nodes are upload → ivideo Multi Angle (comments do not count). Thin first-click: upload + camera preset + run. Distinct from `photo-to-video` (Muse still + MiniMax H3 Spicy locked-camera rain / subject motion), `omni-flash-turntable` (GLM + Omni Flash 1.1 text-to-video studio-plinth orbit), and InfiniteTalk / talking-avatar lipsync. Camera moves; the product stays. Do not add a paid Muse still unless required for Run without upload. | Live video catalog 2026-09-09: `minimax/h3-max/multi-angle/image-to-video` (MiniMax H3 Max Multi Angle; no prefix strip). Image→video only; tags camera-control / multi-angle. Pins `resolution=480p`, `duration=5`, `camera_motion=orbit-right` in `modelOpts` so the runner forwards the additionalParam (same pattern as InfiniteTalk `people`). Catalog also lists orbit-left / push-in / pull-back / rise and optional `camera_trajectory`. Catalog minimum $0.25 (duration/resolution). Short inline prompt matches the marketing example (“Only the camera moves…”). No paid QC. | Radio identity, cyan chevron and slate/rim lighting survive; one continuous camera orbit (or the selected preset); product stays still; no cuts, extra objects or invented motion. Changing the uploaded still or `camera_motion` must change the clip. A later reviewed Run should capture duration, quoted $, and orbit pass/fail. Sample-gallery cover QC is pending. |
| `p-image-upscale` | Keep: dedicated product-still resolution upscale. Distinct from Clean product photo (Muse Edit relights/replaces the backdrop), Product cutout (BiRefNet V2 knocks out the whole background), and Text-selected isolate (`sam3-isolate` / `sam3-image`). First-click story matches the cool pass: matte-charcoal Volt pocket night-ride radio with one electric-cyan lightning-bolt chevron. Detail brief tells the upscaler to preserve exact product shape, materials, markings and cyan chevron; sharpen fine edges only; do not restyle, recolor, invent props, or change composition. No committed still in-repo; the upload is the placeholder (`graphs/p-image-upscale.NOTE.txt`). Working nodes are upload → text Detail brief → P-Image Upscale edit → local 1024 export resize (comments do not count). Custom-endpoint stays retired. Do not use Upscaler, Clarity Crystal/Pro/Flux, or Muse Edit for this graph. | Live image catalog 2026-09-09: `pruna-ai/p-image/upscale` (P-Image Upscale). Text+image → image; sizes are string megapixels `"1"`…`"8"` (not `1k`/`2k`). Pin size `"2"` (2 megapixels) at $0.005; sizes 1–4 are $0.005 and 5–8 are $0.01. `nsfw: false`; max 1 input image. Detail brief is wired to the edit `prompt` port (same pattern as Clean product photo / SAM 3). No LLM. Local resize is $0. No paid QC. | Product shape, materials, markings and cyan chevron survive; composition unchanged; sharper / larger than the input at ~2 MP; no restyle, recolor or invented props. Changing the uploaded still must change the upscale. Inspect the native P-Image Upscale node for sharpness vs the input; Export preview is a local 1024 fit only. Sample-gallery cover QC is pending. Cover may reuse Volt card art later on nanoodle. |
| `transparent-brand-sticker` | Keep: text brief → transparent-background brand sticker/overlay with alpha. Card title **Transparent brand sticker**. Distinct from Favicon concept (`favicon`: GLM Flash + Muse opaque square glyph for a 16px icon) and from Product cutout (`product-cutout` / BiRefNet V2: upload a product still and knock out the background). First-click Brand brief matches the cool Volt night-ride pass: one matte-charcoal field with ONE electric-cyan lightning-bolt chevron for Volt pocket night-ride radio / courier brand; no letters. Flat sticker/overlay, clean alpha, generous transparent margins — not a favicon mockup, not a product photo, not a UI screen. No upload. Working nodes are text Brand brief → image Sticker (comments do not count). No extra LLM — the brief goes straight to Ideogram (favicon uses GLM because it condenses; here the brief already fits). No resize (same as favicon). Custom-endpoint stays retired. Do not use Muse, favicon graph topology, BiRefNet, SAM 3, P-Image Upscale, or Ideogram remove-text. | Live image catalog 2026-09-09: `ideogram-v3-generate-transparent` (Ideogram V3 Generate Transparent). Text→image with transparent background; tags text-to-image / transparent-background. Pin size `1:1` at catalog $0.06 (balanced default for all listed aspects). Catalog `supported_parameters.rendering_speed` lists `flash` / `turbo` / `balanced` / `quality`; flash and turbo are $0.03. Marketing `/api/models` additionalParams match (`rendering_speed` default `balanced`). The nanoodle image node does **not** forward `modelOpts` / additionalParams — `imgExtra` only sends LoRA, seed, CivitAI AIR, and FIBO `aspect_ratio` — so this graph leaves the default and documents $0.06, not a flash pin. `nsfw: false`; max 1 output image. No paid QC. | One charcoal field, one electric-cyan lightning-bolt chevron, no letters; flat sticker/overlay; clean alpha with generous transparent margins; not an opaque favicon square, not a product photo, not a UI screen. Changing Brand brief must change the sticker. Inspect native alpha. Sample-gallery cover QC is pending. |
| `volt-dispatch-infographic` | Keep: text brief → structured Volt courier drop / dispatch **infographic** still. Card title **Volt dispatch card**. Distinct from UI mockup (`render-a-mockup` / Qwen dashboard screenshot), Travel postcard (`night-market-postcard`), FIBO studio still (`fibo-studio-still`), Muse cinematic (`cinematic-character-still`), Transparent brand sticker (`transparent-brand-sticker`), Ideogram V4 Instant poster (`ideogram-v4-instant-poster` / `ideogram/v4/instant`), Remove packaging text (`remove-packaging-text` / `ideogram-v3-remove-text`), Flare arena (`image-model-arena`), Night-ride identity restyle (`h3-identity-restyle`), and Crystal video upscale (`crystal-video-upscale`). First-click Dispatch brief is a Volt night-courier dispatch card: matte charcoal panel, ONE electric-cyan lightning-bolt chevron mark, cyan/magenta accents, clean DROP / ZONE / ETA blocks. Short labels, no text walls. Dark Volt vibe matching the nanoodle editor. No busy collage. Working nodes are text Dispatch brief → image Infographic (comments do not count). No upload. No LLM. No video. Custom-endpoint stays retired. | Live image catalog 2026-09-09: `sensenova-u1-infographic` (SenseNova U1 Infographic). Text→image only (`input_modalities: ["text"]`); tags text-to-image / infographic. Pin size `16:9` at catalog $0.05 (all listed aspects $0.05). Resolutions are aspect ratios (`16:9` / `1:1` / `9:16` / `3:2` / `2:3` / `4:3` / `3:4` / `1:2` / `2:1`), not `1k`/`2k`. `nsfw: false`; max 4 output images — pin `variations: 1`. No additionalParams to forward. No paid QC. | Structured dispatch-card hierarchy with DROP / ZONE / ETA style blocks; one electric-cyan lightning-bolt chevron; charcoal panel with cyan/magenta accents; short labels, no text walls; not a photo, not a UI screenshot, not a postcard. Changing Dispatch brief must change the still. Sample-gallery cover QC is pending. |
| `crystal-video-upscale` | Keep: dedicated video target-megapixel upscale from an uploaded clip. Card title **Crystal video upscale**. First-click is a video-upload placeholder: matte-charcoal Volt pocket night-ride radio with one electric-cyan lightning-bolt chevron (`graphs/crystal-video-upscale.NOTE.txt`). Working nodes are vupload → vedit (comments do not count). Thin first-click: upload clip + run. Distinct from `p-image-upscale` / `pruna-ai/p-image/upscale` (still upscale), from camera-orbit generation (`h3-max-multi-angle` / Omni Flash turntable), from `p-video-rewrite` / `pruna-ai/p-video/edit` (instruction rewrite, keep audio), and from `pixelcut-video-cutout` / `pixelcut/video-background-removal` (video bg knock-out @ black). Custom-endpoint stays retired. Do not retarget this graph for rewrite or cutout. | Live video catalog 2026-09-09: `clarity-ai/crystal-video-upscaler` (Clarity AI Crystal Video Upscaler; no prefix strip). Video→video only; tags upscale / video-to-video. `supported_parameters.target_megapixels` is 1–200 (catalog default 2 ≈ 1080p). Pin cheapest sensible `target_megapixels=1` in vedit `modelOpts` so the runner forwards it (`Object.assign(body, fields.modelOpts)`). Pricing $0.10 per target-megapixel-second, minimum $0.10 (5s @ 1 MP ≈ $0.50). No prompt — catalog is video-only. No paid QC. | Radio identity and cyan chevron survive; clip is sharper / larger than the input at ~1 MP; no restyle, invented motion, cuts or new objects. Changing the uploaded clip must change the upscale. A later reviewed Run should capture duration, quoted $, and sharpness vs the source. Sample-gallery cover QC is pending. |
| `p-video-rewrite` | Keep: prompt-rewrite an uploaded clip and keep source audio. Card title **rewrite the clip**. First-click is a video-upload placeholder: matte-charcoal Volt pocket night-ride radio, charcoal/cyan night-ride take (`graphs/p-video-rewrite.NOTE.txt`). Working nodes are vupload → text Rewrite brief → vedit (comments do not count). Thin first-click: upload clip + rewrite brief + run. Distinct from `crystal-video-upscale` / `clarity-ai/crystal-video-upscaler` (target-megapixel upscale, no prompt), from `pixelcut-video-cutout` / `pixelcut/video-background-removal` (video bg knock-out @ black), from all i2v / turntable / multi-angle motion Keeps (`photo-to-video`, `omni-flash-turntable`, `h3-max-multi-angle`, `grok-imagine-still`, `wan-still-audio`), from `wan-motion-drive` (Animate 2 motion transfer), and from `p-image-upscale`. Custom-endpoint stays retired. No LLM. No Crystal model. | Live video catalog 2026-09-09 (rechecked live 2026-09-10): `pruna-ai/p-video/edit` (P-Video Edit; no prefix strip). Text+image+video→video; this graph wires video + prompt only (no reference still). Tags: video-edit / video-to-video / reference-to-video. Pin `draft=true` and `save_audio=true` in vedit `modelOpts` so the runner forwards them (`Object.assign(body, fields.modelOpts)`). Catalog draft $0.025/s vs full $0.045/s; billing is verified source-video duration, rounded up (min 1s, max 15s). Short source clip (~4s ≈ $0.10 draft). Catalog default `draft=false` — first-click pins draft for cost control. No paid QC. | Radio identity and cyan chevron survive; alley lighting/rain match the brief; source audio preserved; no cuts, new objects, or logo text. Changing the uploaded clip or Rewrite brief must change the output. A later reviewed Run should capture duration, quoted $, and rewrite/audio pass/fail. Sample-gallery cover QC is pending. Cover is the vupload placeholder only. |
| `mirelo-video-foley` | Keep: upload a clip → Mirelo writes matching SFX onto it. Card title **foley the take**; desc **upload a clip — Mirelo writes matching SFX onto it**. First-click is a video-upload placeholder: Volt night-ride alley take (`graphs/mirelo-video-foley.NOTE.txt`). Working nodes are vupload Product clip → vedit Foleyed clip (comments do not count). Thin first-click: upload clip + run. Video→foley, not text→SFX (`night-ride-sfx` / `elevenlabs/sound-effects/v2`) and not Night-ride radio VO (`night-ride-radio-vo` / `xai-tts`). Distinct from Sing, InfiniteTalk, Crystal, P-Video rewrite, and i2v Keeps (`photo-to-video`, `grok-imagine-still`, `wan-still-audio`, `h3-max-multi-angle`, Omni Flash). Custom-endpoint stays retired. No text SFX model, no TTS, no LLM. No paid QC — vupload placeholder only. | Live video catalog 2026-09-09 (rechecked live 2026-09-10): `mirelo-ai/sfx1.6/video-to-video` (Mirelo SFX1.6 Video to Audio; no prefix strip). Video→video with a new synced SFX track; tags video-to-video / audio-for-video / sound-effects. Architecture text+video→video; first-click wires video only (no text SFX brief — the clip drives the foley). Catalog $0.01/s; source videos up to 60s; ~4s ≈ $0.04. Only additionalParam is `seed` (default -1) — not pinned. Sibling `mirelo-ai/sfx1.6/text-to-audio` is text→SFX and is not this job. No paid QC. | Picture stays the uploaded take; new audio is synced Foley/SFX matching on-screen action (alley rain, radio, footsteps — not speech, not a song). Changing the uploaded clip must change the foley. Sample-gallery cover QC is pending. Cover is the vupload placeholder only. |
| `pixelcut-video-cutout` | Keep: dedicated video background knock-out from an uploaded clip. Card title **knock the alley off the clip**; desc **short product take — Pixelcut drops the background**. First-click is a video-upload placeholder: Volt charcoal/cyan radio night-ride take (`graphs/pixelcut-video-cutout.NOTE.txt`). Working nodes are vupload → vedit (comments do not count). Thin first-click: upload clip + run. Pin `background=black` for a preview-friendly solid plate (not transparent). Catalog default `transparent` is a later knob. Distinct from `product-cutout` / `birefnet/v2` (still BiRefNet knock-out), `sam3-isolate` / `sam3-image` (text-selected still isolate), still `background-remover`, `crystal-video-upscale` / `clarity-ai/crystal-video-upscaler` (target-megapixel upscale), `p-video-rewrite` / `pruna-ai/p-video/edit` (instruction rewrite, keep audio), and all i2v / orbit / turntable Keeps. Custom-endpoint stays retired. No LLM. No still cutout. No Crystal / P-Video models. | Live video catalog 2026-09-09 (rechecked live 2026-09-10): `pixelcut/video-background-removal` (Pixelcut Video Background Remover; no prefix strip). Video→video only; tags background-removal / video-to-video / utility / pixelcut / fal. Pin `background=black` in vedit `modelOpts` so the runner forwards it (`Object.assign(body, fields.modelOpts)`). Catalog `supported_parameters.background` options: transparent (default) / black / white / green / blue / magenta / custom (+ `background_color` hex). Pricing $0.022/30 frames (~$0.044 for ~2s at 30 fps). No prompt — catalog is video-only. Do not pin still `background-remover` or `birefnet/v2`. No paid QC. | Radio identity and cyan chevron survive; alley/background gone; first-click output sits on solid black (not alpha); no restyle, invented motion, cuts or new objects. Changing the uploaded clip must change the cutout. A later reviewed Run should capture duration, quoted $, and edge/bg pass/fail. Sample-gallery cover QC is pending. Cover is the vupload placeholder only. |
| `remove-packaging-text` | Keep: dedicated lettering cleanup on a flat graphic / packaging still. Card title **Remove packaging text**. Distinct from Clean product photo (Muse Edit relights/replaces the backdrop), Product cutout (BiRefNet V2 knocks out the whole background), Text-selected isolate (`sam3-isolate` / `sam3-image`), P-Image Upscale (`pruna-ai/p-image/upscale`), and Transparent brand sticker (`transparent-brand-sticker` / `ideogram-v3-generate-transparent`: text→transparent). First-click story matches the cool Volt night-ride pass: a flat Volt pocket night-ride radio packaging / promo card (matte charcoal field, one electric-cyan lightning-bolt chevron) with unwanted promotional lettering to strip. No committed still in-repo; the upload is the placeholder (`graphs/remove-packaging-text.NOTE.txt`). Working nodes are upload → Ideogram Remove Text edit → local 1024 export resize (comments do not count). Custom-endpoint stays retired. Do not use Muse Edit, BiRefNet, SAM 3, P-Image Upscale, `ideogram-v3-generate-transparent`, Ideogram remove-background, or any text-to-image model. | Live image catalog 2026-09-09: `ideogram-v3-remove-text` (Ideogram V3 Remove Text). Image→image only (`input_modalities: ["image"]`); size `auto` only at $0.09. Tags: image-to-image, text-removal, graphic-cleanup, ideogram. `nsfw: false`; max 1 input / 1 output image. Marketing `/api/models` lists the same live id (no prefix strip), cost `auto: 0.09`, no additionalParams. The edit node pins a runner instruction because nanoodle-js refuses a blank edit prompt except for upscalers; catalog `input_modalities` are image-only, so that instruction is not a creative control (same pattern as BiRefNet product-cutout). No LLM. Local resize is $0. No paid QC. | Lettering, captions, watermarks and promotional text are gone; product/artwork, materials, colors, marks and composition survive; removed-text regions look continuous; no restyle, recolor, invented props or layout change. Changing the uploaded still must change the cleanup. Inspect the native Ideogram node; Export preview is a local 1024 fit only. Sample-gallery cover QC is pending. |
| `ideogram-v4-instant-poster` | Keep: text brief → sharp brand/drop poster still with lettering. Card title **Volt drop poster**. Distinct from Transparent brand sticker (`transparent-brand-sticker` / `ideogram-v3-generate-transparent`: alpha sticker, no letters), Favicon concept (`favicon`: GLM Flash + Muse opaque 16px glyph), FIBO studio still / cinematic character still / image-model arena (different models/jobs), and Remove packaging text (`remove-packaging-text` / `ideogram-v3-remove-text`: strips lettering). First-click Poster brief matches the cool Volt night-ride pass: matte-charcoal night-ride drop poster, ONE electric-cyan lightning-bolt chevron, wordmark letters allowed (VOLT / MIDNIGHT DROP), hard cyan rim, dark slate, flat poster graphic — not a product photo. No upload. Working nodes are text Poster brief → image Poster (comments do not count). No extra LLM — the brief goes straight to Ideogram. No resize. Custom-endpoint stays retired. Do not use Muse, FIBO, MiniMax H3, arena contestants, `ideogram-v3-generate-transparent`, or `ideogram-v3-remove-text`. | Live image catalog 2026-09-09: `ideogram/v4/instant` (Ideogram V4 Instant). Text→image; tags text-to-image / typography / design / ideogram / fal. Pin size `1024x1024` at catalog $0.0075 (same cheap tier as `auto`). Catalog `supported_parameters.rendering_speed`: `TURBO` / `BALANCED` / `QUALITY`. TURBO at 1024x1024 is $0.00375. The nanoodle image node does **not** forward `modelOpts` / additionalParams — `imgExtra` only sends LoRA, seed, CivitAI AIR, and FIBO `aspect_ratio` — so this graph leaves the default and documents $0.0075, not a turbo pin. Also lists `ideogram/v4/fast` and `ideogram:4@0` — not this job. `nsfw: false`; max 4 output images; pin 1. No paid QC. | One charcoal field, one electric-cyan lightning-bolt chevron, readable VOLT / MIDNIGHT DROP lettering, hard cyan rim, dark slate; flat poster graphic, not a product photo, not an alpha sticker, not a 16px favicon. Changing Poster brief must change the poster. Sample-gallery cover QC is pending. |
| `virtual-try-on` | Keep: dedicated virtual garment try-on from a person still + garment still + Style brief. Card title **Wear the night kit**; desc **person + garment — FLUX tries the drop on**. First-click uploads are placeholders: mid-shot Volt night courier / model on dark slate, and a flat Volt night-ride jacket or tee with ONE electric-cyan lightning-bolt chevron (`graphs/virtual-try-on.NOTE.txt`). Working nodes are upload Person still + upload Garment still + text Style brief → edit Try-on (comments do not count). Person wires to edit `image` (first / person); garment wires to edit `image2` (second / garment); Style brief wires to `prompt`. No local resize (same as favicon / Clean product photo — this is not a cutout). Custom-endpoint stays retired. No LLM. No video. Distinct from `combine-images` / Muse Edit (product placement into a setting), `edit-a-photo` / Muse Edit (catalog cleanup / backdrop relight), `h3-identity-restyle` / `minimax-h3/image-edit` (identity-preserving scene/outfit restyle — not dedicated garment VTO), `product-cutout` / BiRefNet, `sam3-isolate` / SAM 3, Transparent brand sticker, Ideogram V4 Instant poster, Recraft vector mark, MAI pack type, and SenseNova dispatch infographic. | Live image catalog 2026-09-10: `flux-pro/v1/vto` (FLUX Virtual Try-On). Text+image → image; tags image-edit / virtual-try-on. Pin size `auto` only at catalog **$0.0575**/image. `nsfw: false`; max 1 output image. Exactly two input images: person first (max 2MP), garment second (max 1MP). nanoodle-js `IMG_INPUT_ROLES` for this id is `[person, garment]` on ports `image` / `image2`; runner refuses an unwired slot so a garment cannot silently promote into the person role. Style brief is wired to the edit `prompt` port (same multi-image pattern as `combine-images`). No LLM. No paid QC. | Person identity and pose survive; garment materials and the single cyan lightning-chevron survive; cool night-ride lighting; no extra logos, text walls, props or second people. Changing the person still, garment still, or Style brief must change the try-on. Sample-gallery cover QC is pending — do not invent a paid thumb. |
| `h3-identity-restyle` | Keep: identity-preserving scene/outfit/light restyle from an uploaded person or product still + a short Restyle brief. Card title **Night-ride identity restyle**. First-click Restyle is a Volt night-courier key still (matte charcoal kit, ONE electric-cyan lightning-bolt chevron on the chest, magenta rim + cyan bounce, wet neon alley bokeh, locked medium portrait). Upload is a mid-shot courier / product holder still that needs a cyan-rim night-ride restyle — no committed still in-repo; the upload is the placeholder (`graphs/h3-identity-restyle.NOTE.txt`). Working nodes are upload Still → text Restyle → edit Identity restyle (comments do not count). Prompt wired from Restyle (same pattern as `sam3-isolate` / Clean product photo). No local resize (same as favicon / Clean product photo — this is not a cutout/SAM isolate). Custom-endpoint stays retired. No LLM. No video. Distinct from `edit-a-photo` / Muse Edit (cleanup / backdrop relight), `cinematic-character-still` / `minimax-h3/text-to-image` (T2I, no upload identity), `combine-images` (two-reference placement), `product-cutout` / BiRefNet, `sam3-isolate` / SAM 3, `transparent-brand-sticker`, `remove-packaging-text`, `p-image-upscale`, InfiniteTalk, H3 Max Multi Angle, Flare-in-arena, venice deslop, and character-sprites. | Live image catalog 2026-09-09: `minimax-h3/image-edit` (MiniMax H3 Image Edit; no `wavespeed-ai/` prefix; marketing / upstream still lists `wavespeed-ai/minimax-h3/image-edit`). Text+image → image; identity-preserving restyle. Pin size `1k` at $0.03 (`2k` is $0.09). `nsfw: false`. Catalog allows ≤9 reference images; first-click uses one upload — do not advertise multi-ref as required controls. Restyle text is wired to the edit `prompt` port (same pattern as Clean product photo / SAM 3). No LLM. No paid QC. | Face and body identity survive; kit, ONE cyan lightning-bolt chevron, magenta rim + cyan bounce, and wet neon alley match the Restyle brief; locked medium portrait; no readable text, extra people or invented props. Changing the uploaded still or Restyle text must change the output. Sample-gallery cover QC is pending. |
| `mai-pack-type` | Keep: text brief → readable **product pack lettering** on a packaging sleeve. Card title **pack type that sticks**. Distinct from Ideogram V4 Instant poster (`ideogram-v4-instant-poster`: drop poster), SenseNova Volt dispatch card (`volt-dispatch-infographic`: DROP / ZONE / ETA infographic), Qwen UI mockup (`render-a-mockup`), FIBO studio still, Recraft vector mark (`volt-vector-mark`: no letters), Remove packaging text (strips lettering), and Flare arena. First-click Pack brief is a flat Volt pocket night-ride radio packaging sleeve, 4:3: matte charcoal field, ONE electric-cyan lightning-bolt chevron, hard cyan rim, VOLT / MIDNIGHT DROP / NIGHT CHANNEL. Clean product-pack lettering, generous margins — not a photo of a radio, not a drop poster, not a dispatch card. No upload. Working nodes are text Pack brief → image Pack sleeve (comments do not count). No extra LLM. Custom-endpoint stays retired. Do not pin full `microsoft/mai-image-2.6`. | Live image catalog 2026-09-09: `microsoft/mai-image-2.6-flash` (MAI-Image-2.6 Flash). Faster, lower-cost MAI for product visuals and lettering. Pin size `1152x864` at catalog $0.033075 (~$0.033). Full `microsoft/mai-image-2.6` is $0.06615 at the same size (~2×) — not this first-click. Text+image capable; this graph is text-only (no upload). `nsfw: false`; max 4 output images; pin variations 1. No paid QC; cover honesty: no reviewed sleeve in this first PR. | Readable VOLT / MIDNIGHT DROP / NIGHT CHANNEL pack type; one cyan lightning-bolt chevron; matte charcoal field; hard cyan rim; 4:3 sleeve, not a drop poster or dispatch infographic. Changing Pack brief must change the sleeve. Sample-gallery cover QC is pending. |
| `volt-vector-mark` | Keep: text brief → editable SVG logo mark. Card title **editable volt mark**. Distinct from Favicon concept (`favicon`: GLM Flash + Muse opaque raster glyph for a 16px icon), Transparent brand sticker (`transparent-brand-sticker` / `ideogram-v3-generate-transparent`: PNG alpha sticker, not SVG), Product cutout (`product-cutout` / BiRefNet V2: upload a product still and knock out the background), arena `recraft-v4` raster (`image-model-arena` Contender 4), and Ideogram V4 Instant poster (`ideogram-v4-instant-poster`: lettered drop poster). First-click Mark brief is a single geometric lightning-chevron for Volt pocket night-ride radio: sharp electric-cyan (#00E5FF) on a flat matte-charcoal (#1A1A1E) square; clean vector paths; no letters. No upload. Working nodes are text Mark brief → image SVG mark (comments do not count). No extra LLM. No resize. Custom-endpoint stays retired. Do not use Muse, Ideogram transparent, BiRefNet, or arena `recraft-v4`. | Live image catalog 2026-09-09: `recraft-ai/recraft-v4.1/text-to-vector` (Recraft V4.1 Vector). Text→image tagged text-to-vector / svg; turns prompts into editable SVGs for logos, icons, and illustration systems. Pin size `1024x1024` at catalog $0.08 (same price for listed 1024 aspects). Resolutions: `1024x1024` / `1024x768` / `1024x576` / `768x1024` / `576x1024`. `max_images` 8; pin variations 1. Catalog `nsfw: true` (capability flag); first-click brief is SFW. Distinct from `recraft-v4` (arena raster) and `recraft-ai/recraft-v4.1/text-to-image` (raster, $0.04). No paid QC; cover honesty: no reviewed SVG in this first PR. | One sharp electric-cyan lightning-chevron, charcoal square, clean vector paths, generous margins, no letters, no gradients, no shadows, no radio mockup, no people. Output is a logo-ready editable SVG you can scale — not a PNG cutout sticker and not a 16px favicon raster. Changing Mark brief must change the mark. Sample-gallery cover QC is pending. |
| `alishan-fog-dawn` | Archive under `experiments/retired-gallery/`: duplicate postcard pipeline with hardcoded destination. | No extra model capability or repeatable task over the retained postcard. | Reconsider only for a distinct useful workflow, not another location. |
| `custom-endpoint` | Archive under `experiments/retired-gallery/`: HTTP echo wiring tutorial, decorative mode picker, third-party endpoint. | No model task; the picker never selected its advertised endpoint. | A future gallery entry must do useful work with truthful controls and clear data destination. |

## Verification and limits

The curation pass read every graph and its connections, checked advertised inputs against prompts, and looked up image/video/audio capability and price metadata in the public NanoGPT catalogs on 2026-09-05. All retained model IDs were also checked against the live catalogs by the repository model audit. Catalog metadata supports routing and parameter choices; it does not prove aesthetic quality, prompt adherence, factual accuracy or provider reliability.

README share links are regenerated from exact committed graph JSON and checked with `npm test`. Output acceptance criteria above are requirements, not assertions that new generations passed. The existing character example has committed game/artifact evidence; twelve rewritten workflows now have saved outputs and explicit review notes in the [sample gallery](https://nanoodle.com/examples/gallery/). Image/text inspection, sampled video frames and model-assisted audio review are identified separately; none is a guarantee for future runs. The final avatar uses a male presenter to match the selected male voice; its sampled frames and model-assisted audio transcript passed their stated checks. Precise lip-sync timing is left visible for playback judgment.

Primary catalog sources: [chat](https://nano-gpt.com/api/v1/models?detailed=true), [image](https://nano-gpt.com/api/v1/image-models), [video](https://nano-gpt.com/api/v1/video-models), [audio](https://nano-gpt.com/api/v1/audio-models). Text cost depends on tokens; media estimates exclude text, retries and optional higher settings.

Initial live checks: the revised `deslop` completed at approximately $0.0079 and retained the supplied facts in output review. The first mockup revision failed because the LLM-expanded prompt exceeded the provider’s 800-character cap; the graph now bypasses that LLM and joins a 696-character default brief and style directly. Subsequent output validation is recorded by the session lead.

Image output review: favicon passed for a crisp navy/gold glyph (that review is of the earlier Lumen weather-radio sample; first-click is now Volt courier-night lightning-cyan and the saved webp was not regenerated); FIBO returned a coherent amber pump bottle at 1024×1024 (its unsupported `aspect` field was removed; that review is of the earlier soap-bottle sample; first-click is now Volt night-ride radio editorial still and the saved still was not regenerated); the postcard returned a coherent illustrated night market. All four arena outputs rendered the specified text correctly, but only Grok depicted recognizable tire levers; Krea produced fork-like tools, Recraft pry-bar-like tools and Muse brake-lever-like tools (that review is of the earlier cream FIX A FLAT bicycle-repair workshop sample; first-click is now a Volt night-ride poster — NIGHT RIDE / TUE 9 SEP, charcoal radio, cyan lightning chevron — and the saved arena outputs were not regenerated). This is useful task-specific differentiation, not a universal model ranking. Video API calls completed at $0.21 for photo-to-video and $0.195 for the turntable; completion alone does not establish temporal quality (that turntable review is of the earlier chrome-helmet sample; first-click is now a Volt pocket night-ride radio on a charcoal stone plinth and the saved clip was not regenerated).


Final useful-input checks: replacing the postcard place with a dry sandstone arch produced a desert illustration with no market scenery; replacing the FIBO bottle with a red canvas sneaker produced the requested shoe with no bottle materials. These two runs reported $0.01 and $0.04 respectively. The final 696-character mockup prompt produced the exact requested rows, names, statuses, navigation and totals without invented extra sections (that review is of the earlier repair-shop / forest-green sample; first-click is now Volt courier dispatch charcoal/cyan and the saved preview was not regenerated). Product cleanup and two-reference placement produced plausible images from generated source photos, with exact merchandise fidelity left for source comparison (that product-cleanup review is of the earlier warm-white / soft-studio sample; first-click is now a cool night-ride catalog — matte charcoal seamless, hard cyan rim — and the saved cleanup was not regenerated). The two-reference placement review is of the earlier generic photoshoot-placement sample; first-click is now a Volt pocket night-ride radio in a wet night alley / neon wet-asphalt courier lane, and the saved placement was not regenerated.

The 170-second song completed at $0.225. Gemini 3.8 Flash reviewed its audio for an additional reported $0.00627, transcribed the intended chorus and reported clear acoustic vocals without obvious glitches. That review is of the earlier cozy-repair-shop / analog-folk sample; first-click is now rooftop getaway + cinematic synthwave and the saved MP3 was not regenerated. This is model-assisted audio assessment, not human listening sign-off. The saved audio and generated lyrics let a visitor judge it directly.

Validation: all 13 graphs materialize without warnings in both local JavaScript and Python runners, and their exposed input/output contracts agree. All 28 model-bearing nodes pass the corrected live capability/settings audit. README links round-trip; gallery-to-source parity, JavaScript syntax, five-language coverage and the sample-page asset/hash checks pass. Desktop and 390px mobile browser checks found no page errors or horizontal overflow. The gallery graphs, website sample assets and MCP listings form one release; their public links must resolve together.


Avatar review: the first run exceeded a 240-second deadline with at least $0.28 reported. A longer retry completed with an unspecified presenter appearance that did not match the selected male voice; that output is not featured. The final graph makes the male presenter explicit, and the featured run completed with at least $0.28 reported. Sampled frames preserve identity and plausible facial movement; Gemini 3.8 Flash transcribed the supplied workshop script exactly and reported clear baritone speech. That review is of the earlier bike-mechanic / workshop sample; first-click is now a Volt night-courier product intro and the saved Avatar lipsync MP4 was not regenerated. The sample discloses the several-minute generation time and does not claim frame-accurate lipsync measurement.

The accepted twelve sample runs reported $1.15590 combined, with some avatar prices omitted by the provider. Including discarded attempts, two changed-input checks and three audio-review calls, the reported testing total was approximately $1.79 and is incomplete where price reporting was missing. No generated output from a failed or rejected attempt is featured. The four-model comparison deliberately retains weaker results to show task-specific differences.


## Release verification — 2026-09-06

The provider removed the `wavespeed-ai/` prefix from Krea 2 Turbo, MiniMax H3 Spicy image-to-video and LongCat Avatar 1.5. Active graph IDs now match the live catalogs, with unchanged presets. A fresh full arena run succeeded at $0.118; a fresh five-second tea clip succeeded at $0.21 and retained stable objects in sampled frames (that review is of the earlier tea-on-desk sample; first-click is now Volt night-ride radio on wet asphalt and the saved clip was not regenerated). The avatar retry stopped for insufficient credit after image/speech preparation; its current catalog capabilities match, but a new full run was not verified today. No top-up was performed. The previously reviewed 5 September outputs remain labeled historical, and their original graphs stay downloadable. The sample page's Open workflow links use separate current graphs so retired aliases do not block new runs.

## MiniMax H3 Image gallery addition — 2026-09-08

Public image catalog IDs (no paid generation this pass):

- `minimax-h3/text-to-image` — MiniMax H3 Image. Text-to-image only. $0.02 at `1k`, $0.06 at `2k`. Resolutions `1k`/`2k`; fifteen aspect ratios listed. `nsfw: false`.
- `minimax-h3/image-edit` — MiniMax H3 Image Edit. Text+image, up to nine references, identity-preserving restyle. $0.03 at `1k`. Not used in the new graph.

`cinematic-character-still` pins `minimax-h3/text-to-image` at `1k`. README share-link round-trip is the repo test for this addition. A $0.02 direction QC approved a motorcycle-courier night-alley still; the default Person / Light / Frame briefs now target that energy. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this fourteenth graph remain a follow-up. Do not treat catalog presence as output-quality evidence.

## Fable shaped-plan restoration — 2026-09-09

`fable-five-step` returns after the two-node slop yank. Working nodes are text + Choice + join + LLM (comments do not count). The invoices crime-scene seed is unchanged. No paid generation this pass. README share-link round-trip is the repo test. nanoodle EXAMPLES sync is a follow-up via `node scripts/sync-examples.mjs`. `fable-cold-open` stays yanked.

## BiRefNet V2 product cutout — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `birefnet/v2` — BiRefNet V2. Image→image background removal. $0.01 at `auto`. Tags: background-removal, segmentation. Live `image-models` id has no `fal-ai/` prefix.
- `sam3-image` — SAM 3 Image Segmentation. Text+image. $0.005 at `auto`. Not used in this graph.
- Marketing `/api/models` still lists the upstream ids `fal-ai/birefnet/v2` and `wavespeed-ai/sam3-image`, plus BiRefNet additionalParams `mask_only` (default false) and `refine_foreground` (default true). Pin the live `image-models` ids that resolve today.

`product-cutout` pins `birefnet/v2` at `auto`. README share-link round-trip is the repo test for this addition. No committed Volt still and no paid cutout QC; `graphs/product-cutout.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this sixteenth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`. SAM 3 (`sam3-image`) is a separate Keep graph (`sam3-isolate`).

## Arena Contender 2: GPT Image 2.5 Flare — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `openai/gpt-image-2.5/flare/text-to-image` — GPT Image 2.5 Flare. Everyday HQ text-to-image (shipped 8 Sep). $0.153 at `1k`, $0.255 at `2k`, $0.459 at `4k`. Resolutions `1k`/`2k`/`4k`; fifteen aspect ratios listed. `nsfw: false`.
- `openai/gpt-image-2.5/sunburst/text-to-image` — GPT Image 2.5 Sunburst. Not this swap.

`image-model-arena` Contender 2 (`n4`) replaces `krea-v2/turbo` with Flare at `1k`. Four contestants only: Muse (`meta/muse-image/text-to-image`), Flare, Grok Imagine (`xai/grok-imagine-image/v2.0/text-to-image`), Recraft (`recraft-v4`). Shared brief stays the Volt night-ride poster (exact heading NIGHT RIDE, exact footer TUE 9 SEP). Topology, wiring and compare-four / exact-text judge teaching are unchanged.

README share-link round-trip is the repo test. No paid NanoGPT contender regen. Saved FIX A FLAT / $0.118 arena outputs remain historical (Krea Turbo lineup; that review is of the cream bicycle-repair workshop sample). Catalog presence is not output-quality evidence.

## InfiniteTalk radio take — 2026-09-09

Public video catalog IDs (no paid generation this pass):

- `infinitetalk` — InfiniteTalk. Image+audio→video. Animate one or two people from an image and audio tracks. Catalog minimum ~$0.09; duration and resolution determine pricing. Pins used: `people=single`, `resolution=480p`. Options also include `people=two` and `720p` — not used. No slash prefix.
- `longcat-avatar-1.5` — LongCat Avatar 1.5. Used by `talking-avatar` only. Not in this graph.

`infinitetalk-radio-take` pins `infinitetalk` at 480p / one person. Muse paints a 1:1 mid-shot still; MiniMax Speech 2.8 HD is a first-click audio bootstrap only (calm dry radio line, no music bed) so Run has audio without an upload asset yet. Prefer swapping that Speech node for an Audio upload of a real ~6–10 s night-channel take. Keep audio under ~15 s for a cheap first-click.

This is not `talking-avatar` / Spoken introduction (Muse → MiniMax Speech → LongCat talking-head product intro). Card title **Night-ride radio take**; gallery nav naming must stay distinct from Spoken introduction. The talking-avatar graph was not edited.

README share-link round-trip is the repo test for this addition. No paid Run and no paid NanoGPT spend. A later reviewed Run should capture duration, quoted $, and body-motion pass/fail. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this seventeenth graph remain a follow-up. Do not treat catalog presence as output-quality evidence.

## SAM 3 text-selected isolate — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `sam3-image` — SAM 3 Image Segmentation. Text+image isolate of a named object or region. $0.005 at `auto`. Tags: segmentation, mask, image-to-image. Live `image-models` id has no `wavespeed-ai/` prefix.
- `birefnet/v2` — BiRefNet V2. Image→image background removal. $0.01 at `auto`. Used by `product-cutout` only. Not in this graph.
- Marketing `/api/models` still lists the upstream id `wavespeed-ai/sam3-image`, plus additionalParam `apply_mask` (default true; turn off to return the segmentation mask). Pin the live `image-models` id that resolves today. Leave `apply_mask` at the catalog default; the editor does not require an explicit field.

`sam3-isolate` pins `sam3-image` at `auto`. Isolate text is wired to the edit `prompt` port (same pattern as Clean product photo). This is text-selected isolate from a cluttered scene, not full background removal (that job is Product cutout / BiRefNet). README share-link round-trip is the repo test for this addition. No committed Volt scene still and no paid isolate QC; `graphs/sam3-isolate.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this eighteenth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## P-Image Upscale gallery addition — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `pruna-ai/p-image/upscale` — P-Image Upscale. Text+image → image. String megapixel sizes `"1"`…`"8"` (not `1k`/`2k`). $0.005 for sizes 1–4; $0.01 for 5–8. Pin size `"2"` (2 megapixels, $0.005). `nsfw: false`; max 1 input image.
- `meta/muse-image/edit` — Muse Edit. Used by Clean product photo / `edit-a-photo` only. Not in this graph.
- `birefnet/v2` — BiRefNet V2. Used by `product-cutout` only. Not in this graph.
- `sam3-image` — SAM 3. Used by `sam3-isolate` only. Not in this graph.
- Do not pin Upscaler, Clarity Crystal/Pro/Flux, or Muse Edit for this job.

`p-image-upscale` pins `pruna-ai/p-image/upscale` at size `"2"`. Detail brief is wired to the edit `prompt` port (same pattern as Clean product photo / SAM 3). This is resolution upscale only, not a restyle or cutout. README share-link round-trip is the repo test for this addition. No committed Volt still and no paid upscale QC; `graphs/p-image-upscale.NOTE.txt` records that cover QC is pending. Cover may reuse Volt card art later on nanoodle. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this nineteenth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## MiniMax H3 Max Multi Angle product orbit — 2026-09-09

Public video catalog IDs (no paid generation this pass):

- `minimax/h3-max/multi-angle/image-to-video` — MiniMax H3 Max Multi Angle. Image→video with camera-control presets. Catalog default and cheap pin: `resolution=480p`, `duration=5`, `camera_motion=orbit-right`. Also lists `orbit-left`, `push-in`, `pull-back`, `rise`, optional `camera_trajectory` JSON, and `prompt_expansion_mode` (default `balanced`). Catalog minimum $0.25; duration and resolution determine pricing. Live `video-models` id keeps the `minimax/` prefix.
- `minimax-h3/image-to-video-spicy` — MiniMax H3 Spicy. Used by `photo-to-video` only. Not in this graph.
- `google/gemini-omni-flash/v1.1` — Omni Flash 1.1 text-to-video. Used by `omni-flash-turntable` only. Not in this graph.
- `infinitetalk` — InfiniteTalk. Used by `infinitetalk-radio-take` only. Not in this graph.
- Marketing `/api/models` additionalParams match the live `video-models` supported_parameters. Pin `camera_motion` in ivideo `modelOpts` so nanoodle-js forwards it (`Object.assign(body, fields.modelOpts)`); `resolution` and `duration` stay top-level ivideo dims.

`h3-max-multi-angle` pins `minimax/h3-max/multi-angle/image-to-video` at 480p / 5s / orbit-right. Working nodes are upload → ivideo (comments do not count). A short inline prompt matches the marketing example (“Only the camera moves: orbit around the subject, preserving the scene.”) — no extra text node. This is a product-reveal camera orbit from an uploaded still, not locked-camera rain (`photo-to-video`) and not a text-to-video studio-plinth concept (`omni-flash-turntable`). README share-link round-trip is the repo test for this addition. No committed Volt still and no paid orbit QC; `graphs/h3-max-multi-angle.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this twentieth graph remain a follow-up. Do not treat catalog presence as output-quality evidence.

## Deslop burstiness / ZeroGPT — 2026-09-09

The prior gallery CLEAN scored worse on free ZeroGPT than the slop draft (29.2% AI vs ~20.9%). First rewrite (`n2`) keeps the burstiness/perplexity system prompt (four-word punches + one longer clause, concrete nouns, no filler) and is now pinned to live catalog `venice-uncensored` ($0.40/$1.80 per 1M, 2026-09-09) to show an uncensored/abliterated model and retire `openai/gpt-4o-mini` (too old). Topology stays rewrite → Terra fact/clarity review → Grok apply.

Committed CLEAN_v10 notice (exact text in `graphs/deslop.NOTE.txt`) from `venice-uncensored` + burstiness measured 0% AI GPT on zerogpt.com ("Your Text is Human written"). The unchanged slop draft measured ~20.9% AI GPT. Same burstiness prompt, other first-rewrite candidates: qwen3.6 7.1% AI; gemma-4 44.6% AI. GPTZero is pending captcha — do not invent other detector scores. No paid NanoGPT image spend. Review still checks writing and facts, not authorship.

## Ideogram V3 transparent brand sticker — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `ideogram-v3-generate-transparent` — Ideogram V3 Generate Transparent. Text→image with a transparent background. Best for logos, stickers, overlays, and other assets that need a clean alpha channel from the prompt. Pin size `1:1` at catalog $0.06 (balanced default; all listed aspects $0.06). Catalog `supported_parameters.rendering_speed`: `flash` / `turbo` / `balanced` / `quality`. Flash and turbo are $0.03; quality is $0.09. `nsfw: false`; max 1 output image.
- `meta/muse-image/text-to-image` — Muse. Used by Favicon concept / `favicon` only. Not in this graph.
- `birefnet/v2` — BiRefNet V2. Used by `product-cutout` only. Not in this graph.
- `sam3-image` — SAM 3. Used by `sam3-isolate` only. Not in this graph.
- `ideogram-v3-remove-text` — Ideogram V3 Remove Text. Image→image lettering cleanup. Not this job.
- Marketing `/api/models` lists the same live id `ideogram-v3-generate-transparent` (no prefix strip) plus additionalParam `rendering_speed` (default `balanced`). Do not pin flash in `modelOpts`: the nanoodle image node does not forward `modelOpts` / additionalParams. `imgExtra` in nanoodle-js only sends LoRA, seed, CivitAI AIR, and FIBO `aspect_ratio`. Leave the catalog default and document $0.06.

`transparent-brand-sticker` pins `ideogram-v3-generate-transparent` at `1:1`. Working nodes are text Brand brief → image Sticker (comments do not count). No LLM — the first-click brief is already a complete Ideogram prompt (favicon uses GLM because it condenses a short brand line; here the brief stays under prompt limits). No upload. No local resize.

This is text-to-image transparent sticker/overlay generation, not an opaque square favicon glyph (`favicon` / GLM Flash + Muse) and not an uploaded product cutout (`product-cutout` / BiRefNet). Card title **Transparent brand sticker**. First-click Brand brief is a matte-charcoal field with ONE electric-cyan lightning-bolt chevron for Volt pocket night-ride radio / courier brand; no letters; flat sticker/overlay; clean alpha; generous transparent margins. Favicon, product-cutout, and other Keep graphs were not edited.

README share-link round-trip is the repo test for this addition. No paid Ideogram / NanoGPT run and no committed sticker asset; `graphs/transparent-brand-sticker.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this twenty-first graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## Ideogram V3 Remove Text packaging cleanup — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `ideogram-v3-remove-text` — Ideogram V3 Remove Text. Image→image lettering cleanup. Upload a flat graphic; removes lettering while preserving the surrounding design. Pin size `auto` only at catalog $0.09. `nsfw: false`; max 1 input / 1 output image. Tags: image-to-image, text-removal, graphic-cleanup, ideogram.
- `ideogram-v3-generate-transparent` — Ideogram V3 Generate Transparent. Used by Transparent brand sticker / `transparent-brand-sticker` only. Not in this graph.
- `meta/muse-image/edit` — Muse Edit. Used by Clean product photo / `edit-a-photo` only. Not in this graph.
- `birefnet/v2` — BiRefNet V2. Used by `product-cutout` only. Not in this graph.
- `sam3-image` — SAM 3. Used by `sam3-isolate` only. Not in this graph.
- `pruna-ai/p-image/upscale` — P-Image Upscale. Used by `p-image-upscale` only. Not in this graph.
- Marketing `/api/models` lists the same live id `ideogram-v3-remove-text` (no prefix strip), cost `auto: 0.09`, defaultSettings `nImages: 1` / `resolution: auto`. No additionalParams to pin.

`remove-packaging-text` pins `ideogram-v3-remove-text` at `auto`. Working nodes are upload Packaging still → edit Remove lettering → local 1024 Export preview (comments do not count). Topology matches `product-cutout` almost 1:1 with a different model, prompt and names. The edit prompt is a runner instruction because nanoodle-js refuses a blank edit prompt except for upscalers; catalog `input_modalities` are image-only, so that prompt is not a creative control.

This is lettering cleanup on a flat graphic / packaging still, not a relit catalog plate (`edit-a-photo` / Muse Edit), not a transparent cutout (`product-cutout` / BiRefNet), not text-selected isolate (`sam3-isolate` / SAM 3), not resolution upscale (`p-image-upscale`), and not a text-to-transparent sticker (`transparent-brand-sticker` / `ideogram-v3-generate-transparent`). Card title **Remove packaging text**. First-click upload story is a flat Volt pocket night-ride radio packaging / promo card (matte charcoal field, one electric-cyan lightning-bolt chevron) with unwanted promotional lettering to strip. Other Keep graphs were not edited.

README share-link round-trip is the repo test for this addition. No paid Ideogram / NanoGPT run and no committed packaging still; `graphs/remove-packaging-text.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this twenty-second graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## Ideogram V4 Instant Volt drop poster — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `ideogram/v4/instant` — Ideogram V4 Instant. Fastest Ideogram V4 text-to-image endpoint; tuned for poster, logo, product mockup, and typography-heavy design. Pin size `1024x1024` at catalog $0.0075 (cheap tier; `auto` is the same $0.0075). Catalog `supported_parameters.rendering_speed`: `TURBO` / `BALANCED` / `QUALITY`. TURBO at 1024x1024 is $0.00375; QUALITY is $0.0125. `nsfw: false`; max 4 output images. Resolutions also include 1536 and 2048 sizes at higher prices.
- `ideogram-v3-generate-transparent` — Ideogram V3 Generate Transparent. Used by Transparent brand sticker / `transparent-brand-sticker` only. Not in this graph.
- `ideogram-v3-remove-text` — Ideogram V3 Remove Text. Used by Remove packaging text / `remove-packaging-text` only. Not in this graph.
- `ideogram/v4/fast` — Ideogram V4 Fast. Not this job.
- `ideogram:4@0` — Ideogram 4.0. Not this job.
- `meta/muse-image/text-to-image` — Muse. Used by Favicon concept / `favicon` only. Not in this graph.
- Do not pin TURBO in `modelOpts`: the nanoodle image node does not forward `modelOpts` / additionalParams. `imgExtra` in nanoodle-js only sends LoRA, seed, CivitAI AIR, and FIBO `aspect_ratio`. Leave the catalog default and document $0.0075.

`ideogram-v4-instant-poster` pins `ideogram/v4/instant` at `1024x1024`. Working nodes are text Poster brief → image Poster (comments do not count). No LLM — the first-click brief is already a complete Ideogram prompt. No upload. No local resize.

This is a typography-heavy brand/drop poster still, not an alpha sticker with no letters (`transparent-brand-sticker`), not an opaque 16px glyph (`favicon`), not a FIBO / H3 / arena still, and not lettering cleanup (`remove-packaging-text`). Card title **Volt drop poster**. First-click Poster brief is a matte-charcoal Volt night-ride drop poster with ONE electric-cyan lightning-bolt chevron, VOLT / MIDNIGHT DROP letters, hard cyan rim, dark slate — flat poster graphic, not a product photo. Other Keep graphs were not edited.

README share-link round-trip is the repo test for this addition. No paid Ideogram / NanoGPT run and no committed poster asset; `graphs/ideogram-v4-instant-poster.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this twenty-third graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## Clarity Crystal video upscale — 2026-09-09

Public video catalog IDs (no paid generation this pass):

- `clarity-ai/crystal-video-upscaler` — Clarity AI Crystal Video Upscaler. Video→video target-megapixel upscale. `supported_parameters.target_megapixels` 1–200; catalog default 2 (≈1080p). Pin cheapest sensible `1`. Pricing $0.10 per target-megapixel-second, minimum $0.10 (5s @ 1 MP ≈ $0.50). Live `video-models` id keeps the `clarity-ai/` prefix. Tags: upscale / video-to-video.
- `pruna-ai/p-image/upscale` — P-Image Upscale. Used by `p-image-upscale` only. Not in this graph.
- `minimax/h3-max/multi-angle/image-to-video` — MiniMax H3 Max Multi Angle. Used by `h3-max-multi-angle` only. Not in this graph.
- `google/gemini-omni-flash/v1.1` — Omni Flash 1.1 text-to-video. Used by `omni-flash-turntable` only. Not in this graph.

`crystal-video-upscale` pins `clarity-ai/crystal-video-upscaler` at `target_megapixels=1` in vedit `modelOpts`. Working nodes are vupload Product clip → vedit Upscaled clip (comments do not count). No prompt — catalog `input_modalities` are video-only. No LLM. No local resize.

This is video target-megapixel upscale of an uploaded clip, not still upscale (`p-image-upscale` / `pruna-ai/p-image/upscale`) and not a camera-orbit generate (`h3-max-multi-angle` / Omni Flash turntable). Card title **Crystal video upscale**. First-click upload story is a matte-charcoal Volt pocket night-ride radio clip with one electric-cyan lightning-bolt chevron. Other Keep graphs were not edited.

README share-link round-trip is the repo test for this addition. No committed Volt clip and no paid upscale QC; `graphs/crystal-video-upscale.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this twenty-fourth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`. Distinct from `p-video-rewrite` / `pruna-ai/p-video/edit` (instruction rewrite, keep audio) — do not retarget this graph.

## P-Video Edit rewrite — 2026-09-09

Public video catalog IDs (no paid generation this pass):

- `pruna-ai/p-video/edit` — P-Video Edit. Instruction-based editing for videos up to 15 seconds, with optional reference-image guidance, Draft and Full quality modes, prompt enhancement, and source-audio preservation. Live `video-models` id keeps the `pruna-ai/` prefix. Tags: video-edit / video-to-video / reference-to-video. Input modalities: text + image + video. Pin `draft=true` (catalog default is `false`) and `save_audio=true` (catalog default `true`; pin so the runner forwards it). Live quote 2026-09-09 / rechecked 2026-09-10: draft $0.025/s vs full $0.045/s; billing is verified source-video duration, rounded up (min 1s, max 15s). Short source clip (~4s ≈ $0.10 draft; ~4s full ≈ $0.18). Also lists `prompt_upsampling` (default true) and `seed` — not pinned.
- `clarity-ai/crystal-video-upscaler` — Clarity AI Crystal Video Upscaler. Used by `crystal-video-upscale` only. Not in this graph.
- `pruna-ai/p-image/upscale` — P-Image Upscale. Used by `p-image-upscale` only. Not in this graph.
- `minimax/h3-max/multi-angle/image-to-video` — MiniMax H3 Max Multi Angle. Used by `h3-max-multi-angle` only. Not in this graph.
- `minimax-h3/image-to-video-spicy` — MiniMax H3 Spicy. Used by `photo-to-video` only. Not in this graph.
- `google/gemini-omni-flash/v1.1` — Omni Flash 1.1 text-to-video. Used by `omni-flash-turntable` only. Not in this graph.
- Sibling P-Video ids `pruna-ai/p-video/text-to-video`, `pruna-ai/p-video/image-to-video`, `pruna-ai/p-video/animate`, and `pruna-ai/p-video/avatar` are not this job.

`p-video-rewrite` pins `pruna-ai/p-video/edit` at `draft=true` + `save_audio=true` in vedit `modelOpts`. Working nodes are vupload Product clip → text Rewrite brief → vedit Rewritten clip (comments do not count). Rewrite brief is wired to the vedit `prompt` port. No LLM. No still upscale. No Crystal model. No reference-image upload.

This is prompt-rewrite of an uploaded clip with source-audio keep, not Crystal target-megapixel upscale (`crystal-video-upscale` / `clarity-ai/crystal-video-upscaler`), not still upscale (`p-image-upscale`), and not an i2v / turntable / multi-angle motion pin (`photo-to-video`, `omni-flash-turntable`, `h3-max-multi-angle`, `grok-imagine-still`, `wan-still-audio`), and not `wan-motion-drive` (Animate 2 motion transfer). Card title **rewrite the clip**. First-click upload story is a matte-charcoal Volt pocket night-ride radio / charcoal-cyan night-ride take. Other Keep graphs were not edited. Draft test-pins (`deslop`), Flare/Fable showpieces, and custom-endpoint stay as they are.

README share-link round-trip is the repo test for this addition. No committed Volt clip and no paid rewrite QC; `graphs/p-video-rewrite.NOTE.txt` records that cover QC is pending (vupload placeholder only). Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this thirtieth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## MiniMax H3 Image Edit identity restyle — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `minimax-h3/image-edit` — MiniMax H3 Image Edit. Text+image → image. Identity-preserving restyle from up to nine references (scene, outfit, light). Pin size `1k` at $0.03; `2k` is $0.09. `nsfw: false`. Live `image-models` id has no `wavespeed-ai/` prefix.
- `minimax-h3/text-to-image` — MiniMax H3 Image. Used by Cinematic character still / `cinematic-character-still` only. Not in this graph.
- `meta/muse-image/edit` — Muse Edit. Used by Clean product photo / `edit-a-photo` only. Not in this graph.
- Marketing / upstream still lists `wavespeed-ai/minimax-h3/image-edit`. Pin the live `image-models` id that resolves today.

`h3-identity-restyle` pins `minimax-h3/image-edit` at `1k`. Working nodes are upload Still → text Restyle → edit Identity restyle (comments do not count). Restyle text is wired to the edit `prompt` port (same pattern as Clean product photo / SAM 3). First-click uses one upload; catalog allows ≤9 refs — do not advertise multi-ref as required controls. No local resize (not a cutout/SAM isolate). No LLM. No video.

This is identity-preserving scene/outfit/light restyle from an uploaded still, not Muse Edit cleanup (`edit-a-photo`), not T2I character key-art (`cinematic-character-still` / `minimax-h3/text-to-image`), and not two-reference placement (`combine-images`). Card title **Night-ride identity restyle**. First-click Restyle is a Volt night-courier key still: same face and body, matte charcoal kit, ONE electric-cyan lightning-bolt chevron on the chest, magenta rim + cyan bounce, wet neon alley bokeh, locked medium portrait. Upload story is a mid-shot courier / product holder still that needs a cyan-rim night-ride restyle. Other Keep graphs were not edited.

README share-link round-trip is the repo test for this addition. No paid NanoGPT run and no committed still; `graphs/h3-identity-restyle.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this twenty-fifth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## SenseNova U1 Infographic Volt dispatch card — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `sensenova-u1-infographic` — SenseNova U1 Infographic. Text→image structured infographic from a layout / content / typography / visual-style brief. Pin size `16:9` at catalog $0.05 (all listed aspects $0.05). Resolutions are aspect ratios: `16:9` / `1:1` / `9:16` / `3:2` / `2:3` / `4:3` / `3:4` / `1:2` / `2:1`. `nsfw: false`; max 4 output images. Tags: text-to-image, infographic. `input_modalities: ["text"]`. Owned by SenseTime.
- `qwen-image-3-pro` — Qwen Image 3 Pro. Used by UI mockup / `render-a-mockup` only. Not in this graph.
- `meta/muse-image/text-to-image` — Muse. Used by Travel postcard / Favicon / cinematic neighbors. Not in this graph.
- `ideogram-v3-generate-transparent` — Ideogram V3 Generate Transparent. Used by Transparent brand sticker only. Not in this graph.
- `ideogram-v3-remove-text` — Ideogram V3 Remove Text. Used by Remove packaging text only. Not in this graph.
- `ideogram/v4/instant` — Ideogram V4 Instant. Used by Volt drop poster / `ideogram-v4-instant-poster` only. Not this job.
- `minimax-h3/image-edit` — MiniMax H3 Image Edit. Used by Night-ride identity restyle / `h3-identity-restyle` only. Not in this graph.
- `clarity-ai/crystal-video-upscaler` — Clarity Crystal Video Upscaler. Used by Crystal video upscale / `crystal-video-upscale` only. Not in this graph.
- `openai/gpt-image-2.5/flare/text-to-image` — GPT Image 2.5 Flare. Used by Compare image models / `image-model-arena` only. Not in this graph.
- Marketing `/api/models` is not required for this pin: live `image-models` lists `sensenova-u1-infographic` with no prefix strip and no additionalParams. Size is the aspect pin.

`volt-dispatch-infographic` pins `sensenova-u1-infographic` at `16:9`. Working nodes are text Dispatch brief → image Infographic (comments do not count). No LLM — the first-click brief is already a complete short infographic prompt. No upload. No video. No local resize.

This is a structured Volt courier drop / dispatch **infographic** still, not a UI dashboard mockup (`render-a-mockup`), not a travel postcard (`night-market-postcard`), not a FIBO product still, not a Muse cinematic hero, not a transparent sticker, not an Ideogram V4 Instant poster (`ideogram-v4-instant-poster`), not lettering cleanup (`remove-packaging-text`), not Night-ride identity restyle (`h3-identity-restyle`), not Crystal video upscale, and not the Flare arena. Card title **Volt dispatch card**. First-click Dispatch brief is a matte charcoal panel with ONE electric-cyan lightning-bolt chevron and cyan/magenta DROP / ZONE / ETA blocks; short labels only. Other Keep graphs were not edited.

README share-link round-trip is the repo test for this addition. No paid SenseNova / NanoGPT run and no committed infographic asset; `graphs/volt-dispatch-infographic.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this twenty-sixth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## ElevenLabs Sound Effects V2 night-ride sting — 2026-09-09

Public audio catalog IDs (no paid generation this pass):

- `elevenlabs/sound-effects/v2` — ElevenLabs Sound Effects V2. Text→audio SFX (and advertised seamless loops). $0.002/s; pin `duration=4` (~$0.008). Catalog range 0.5–22s. Category `audio_generation`. `capabilities.text_to_audio: true`. Live `audio-models` id keeps the `elevenlabs/` prefix.
- `elevenlabs/music` — ElevenLabs Music. Text→music, $0.01/s, min 5s. Not this job.
- `mureka-ai/mureka-v9.5/generate-song` — Mureka Generate Song. Used by Closing-credits song / `sing` only. Not in this graph.
- `Elevenlabs-Turbo-V2.5` / `Elevenlabs-V3` — ElevenLabs TTS. Used by no Keep here; talking-avatar / InfiniteTalk bootstrap use MiniMax Speech. Not this job.
- Music-node already forwards `duration` when the catalog advertises `min_duration` / `max_duration` (this model does).

`night-ride-sfx` pins `elevenlabs/sound-effects/v2` at duration 4. Working nodes are text SFX brief → music Sting (comments do not count). No LLM — the first-click brief is already a complete SFX prompt. No upload. No image/video.

This is a short Volt radio / cyan-thunder sting, not a closing-credits song (`sing` / Mureka), not TTS / voice, and not video→foley (`mirelo-video-foley` / `mirelo-ai/sfx1.6/video-to-video`). Card title **Night-ride SFX**. First-click SFX brief: wet neon alley pulse, low radio static bloom into one electric-cyan thunder hit, then tight decay; pocket-radio energy, ~4s, no music bed, no vocals, no dialogue. Other Keep graphs were not edited.

README share-link round-trip is the repo test for this addition. No paid ElevenLabs / NanoGPT run and no committed sting; `graphs/night-ride-sfx.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this twenty-seventh graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## Recraft V4.1 Volt vector mark — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `recraft-ai/recraft-v4.1/text-to-vector` — Recraft V4.1 Vector. Text→editable SVG for logos, icons, and illustration systems. Pin size `1024x1024` at catalog $0.08 (all listed 1024 aspects $0.08). Tags: text-to-vector, svg, recraft, typography, design. Resolutions: `1024x1024` / `1024x768` / `1024x576` / `768x1024` / `576x1024`. `max_images` / `max_output_images` 8; pin variations 1. Catalog `nsfw: true` is a capability flag; the first-click brief is SFW.
- `recraft-ai/recraft-v4.1/text-to-image` — Recraft V4.1 raster. $0.04 at 1024x1024. Not this job.
- `recraft-v4` — Recraft V4 raster. Used by Compare image models / `image-model-arena` Contender 4 only. Not in this graph.
- `recraft-ai/recraft-v4.1-pro/text-to-vector` — Recraft V4.1 Pro Vector. $0.30 at 2048. Not this job.
- `meta/muse-image/text-to-image` — Muse. Used by Favicon concept / `favicon` only. Not in this graph.
- `ideogram-v3-generate-transparent` — Ideogram V3 Generate Transparent. Used by Transparent brand sticker / `transparent-brand-sticker` only. Not in this graph.
- `birefnet/v2` — BiRefNet V2. Used by Product cutout / `product-cutout` only. Not in this graph.
- `ideogram/v4/instant` — Ideogram V4 Instant. Used by Volt drop poster / `ideogram-v4-instant-poster` only. Not in this graph.

`volt-vector-mark` pins `recraft-ai/recraft-v4.1/text-to-vector` at `1024x1024`. Working nodes are text Mark brief → image SVG mark (comments do not count). No LLM — the first-click brief is already a complete vector-mark prompt. No upload. No local resize.

This is an editable SVG logo mark, not a PNG alpha sticker (`transparent-brand-sticker`), not a Muse 16px favicon glyph (`favicon`), not a BiRefNet product cutout (`product-cutout`), not arena `recraft-v4` raster, and not a lettered Ideogram V4 Instant poster (`ideogram-v4-instant-poster`). Card title **editable volt mark**; card desc **sharp cyan chevron — SVG you can scale**. First-click Mark brief is a single geometric lightning-chevron for Volt pocket night-ride radio: sharp electric-cyan (#00E5FF) on a flat matte-charcoal (#1A1A1E) square; clean vector paths; bold silhouette; generous margins; no letters, gradients, shadows, radio mockup or people. Other Keep graphs were not edited.

README share-link round-trip is the repo test for this addition. No paid Recraft / NanoGPT run and no committed SVG asset; `graphs/volt-vector-mark.NOTE.txt` records that cover QC is pending. Cover honesty: this first PR has no reviewed sample. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this twenty-eighth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## Grok Imagine Video 1.5 still→video — 2026-09-09

Public video catalog IDs (no paid generation this pass):

- `xai/grok-imagine-video/v1.5/image-to-video` — Grok Imagine Video 1.5. Text, a starting image, or reference images. Live `video-models` id keeps the `xai/` + `/v1.5/` path. Image-to-video supports 480p and 720p (1080p is text-to-video only). Duration options `1`–`15` (default `6`). Image-mode (wired still) quote 2026-09-09: $0.204/s at 480p, $0.34/s at 720p, plus $0.017 `image_input_fee`. Catalog default is 6s / 720p (~$2.06 video). This pin chooses 480p / 4s: 4 × $0.204 + $0.017 = $0.833 video. Muse still $0.01. Text-mode 480p is $0.08/s — not this job.
- `grok-imagine-video` — older Grok Imagine Video (v1). Not this pin.
- `minimax-h3/image-to-video-spicy` — MiniMax H3 Spicy. Used by `photo-to-video` only. Not in this graph.
- Do not pin Wan or Seedance for this job.

`grok-imagine-still` pins `xai/grok-imagine-video/v1.5/image-to-video` at 480p / 4s. Working nodes are text Still brief → Muse `meta/muse-image/text-to-image` @ 16:9 → ivideo (motion brief wired to prompt). Comments do not count.

This is still → Grok 1.5 motion, not Animate a product still (`photo-to-video`: Muse + MiniMax H3 Spicy locked-camera asphalt rain, 5s / 480p / $0.20). Card title **frame wakes up**. First-click is Volt charcoal/cyan radio on a rooftop ledge — locked cam, subtle rain, one cyan pulse. The photo-to-video graph was not edited. Crystal upscale, Ideogram Instant, identity restyle, Recraft vector mark, and the other concurrent Keep graphs stay on their own cards.

README share-link round-trip is the repo test for this addition. No paid Run and no paid NanoGPT spend; `graphs/grok-imagine-still.NOTE.txt` records that cover QC is pending — no fake paid thumb. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this twenty-ninth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## MAI-Image-2.6 Flash pack type — 2026-09-09

Public image catalog IDs (no paid generation this pass):

- `microsoft/mai-image-2.6-flash` — MAI-Image-2.6 Flash. Faster, lower-cost MAI for product visuals, creative drafts, and lettering. Pin size `1152x864` at catalog $0.033075 (~$0.033). Same cheap tier as `1024x1024` / `auto`. Tags: microsoft, text-to-image, image-to-image. `nsfw: false`; max 4 output images. Resolutions include `1152x864` (4:3).
- `microsoft/mai-image-2.6` — full MAI-Image-2.6. $0.06615 at `1152x864` (~2× Flash). Not this first-click.
- `ideogram/v4/instant` — Ideogram V4 Instant. Used by Volt drop poster / `ideogram-v4-instant-poster` only. Not this job.
- `sensenova-u1-infographic` — SenseNova U1 Infographic. Used by Volt dispatch card / `volt-dispatch-infographic` only. Not this job.
- `qwen-image-3-pro` — Qwen Image 3 Pro. Used by UI mockup / `render-a-mockup` only. Not this job.

`mai-pack-type` pins `microsoft/mai-image-2.6-flash` at `1152x864`. Working nodes are text Pack brief → image Pack sleeve (comments do not count). No LLM. No upload. No local resize.

This is readable product pack lettering on a sleeve, not a drop poster (`ideogram-v4-instant-poster`), not a SenseNova dispatch infographic (`volt-dispatch-infographic`), and not a Qwen UI mockup (`render-a-mockup`). Card title **pack type that sticks**; card desc **Volt sleeve type — Microsoft MAI Flash nails the letters**. First-click Pack brief is a flat Volt pocket night-ride radio packaging sleeve, 4:3: matte charcoal, ONE electric-cyan lightning-bolt chevron, hard cyan rim, VOLT / MIDNIGHT DROP / NIGHT CHANNEL. Other Keep graphs were not edited.

README share-link round-trip is the repo test for this addition. No paid MAI / NanoGPT run and no committed sleeve asset; `graphs/mai-pack-type.NOTE.txt` records that cover QC is pending. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this thirty-first graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## SpaceXAI TTS night-ride radio VO — 2026-09-10

Public audio catalog IDs (no paid generation this pass):

- `xai-tts` — SpaceXAI TTS. Affordable Runware-hosted SpaceXAI text-to-speech with five voices, inline expressive controls, and multilingual auto-detection. Live `audio-models` id is `xai-tts` (no prefix strip). `architecture.modality` text→audio; `capabilities.text_to_speech: true`; category `audio_tts`. Voices: Eve, Ara, Leo, Rex, Sal. Pin `voice=Leo`. Pricing $0.0165 per 1k chars; `max_chars` 5000. Rechecked live 2026-09-10.
- `elevenlabs/sound-effects/v2` — ElevenLabs Sound Effects V2. Used by Night-ride SFX / `night-ride-sfx` only. Not in this graph.
- `Minimax-Speech-2.8-HD` — MiniMax Speech 2.8 HD. Used by Spoken introduction / `talking-avatar` and InfiniteTalk Speech bootstrap. Not this job.
- `Elevenlabs-Turbo-V2.5` / `Elevenlabs-V3` — ElevenLabs TTS. Not this job.
- `Kokoro-82m` — Kokoro 82M. Not this job.
- `Omnivoice` — Omnivoice. Not this job.
- `mureka-ai/mureka-v9.5/generate-song` — Mureka Generate Song. Used by Closing-credits song / `sing` only. Not in this graph.
- TTS node forwards `voice` when the catalog lists voices (this model does). No Delivery text node: skip `instructions` so the graph stays comment + Spoken script → Speech.

`night-ride-radio-vo` pins `xai-tts` with `voice=Leo` and `response_format=mp3`. Working nodes are text Spoken script → tts Speech (comments do not count). No LLM — the first-click script is already the spoken line. No upload. No image/video.

This is speech only (text → audio), not a talking-head product intro (`talking-avatar`: Muse + MiniMax Speech 2.8 HD + LongCat), not a still+audio speaking clip (`infinitetalk-radio-take`), not an SFX sting (`night-ride-sfx` / `elevenlabs/sound-effects/v2`), not video→foley (`mirelo-video-foley` / `mirelo-ai/sfx1.6/video-to-video`), not a closing-credits song (`sing` / Mureka), and not speech→text (`whisper-pull-words` / `Whisper-Large-V3`). Card title **Night-ride radio VO**. Do not retarget this graph for STT. First-click Spoken script: “Volt is live on the night board. Grab your pocket radio, take the ridge handoff, and I'll keep the cyan channel clear until you're docked.” Cool Volt night-ride dispatch — not a cozy workshop. Other Keep graphs were not edited.

README share-link round-trip is the repo test for this addition. No paid xai-tts / NanoGPT run and no committed VO sample; `graphs/night-ride-radio-vo.NOTE.txt` records that cover QC is pending. Do not invent a reviewed mp3. Cover for nanoodle EXAMPLES sync later may reuse talking-avatar / courier card art. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this thirty-second graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## Wan 3.0 still→video with synced audio — 2026-09-09

Public video catalog IDs (no paid generation this pass):

- `alibaba/wan-3.0/image-to-video` — Wan 3.0 Image-to-Video. Animate a first-frame image into a cinematic video with optional last-frame guidance, synchronized audio, deep-thinking controls, and 2–30 second output. Live `video-models` id keeps the `alibaba/wan-3.0/` path. Image→video; tags wan / alibaba / image-to-video / audio. Resolutions 480p / 720p / 1080p (default 720p). Duration number 2–30 (default 5). `enable_audio` switch default true; `thinking_mode` switch default false. Pricing 2026-09-09: $0.07/s at 480p, $0.13/s at 720p, $0.28/s at 1080p. This pin chooses 480p / 2s / enable_audio=true / thinking_mode=false: 2 × $0.07 = $0.14 video. Muse still $0.01. Catalog default 5s / 720p ≈ $0.65 video — do not pin that.
- `alibaba/wan-3.0/text-to-video` — Wan 3.0 Text-to-Video. Not this job.
- `alibaba/wan-3.0/reference-to-video` — Wan 3.0 Reference-to-Video. Not this job.
- `alibaba/wan-3.0-prime` — Wan 3.0 Prime router. Not this job.
- `xai/grok-imagine-video/v1.5/image-to-video` — Grok Imagine Video 1.5. Used by `grok-imagine-still` only. Not in this graph.
- `minimax-h3/image-to-video-spicy` — MiniMax H3 Spicy. Used by `photo-to-video` only. Not in this graph.
- Do not pin Seedance, Grok, Flare, Fable, or `openai/gpt-4o-mini` for this job.

`wan-still-audio` pins `alibaba/wan-3.0/image-to-video` at 480p / 2s (`duration` is a catalog number, stored as `2` not `"2"`) with `enable_audio=true` and `thinking_mode=false` in ivideo `modelOpts` (duration is also repeated there so the runner forwards it). The live editor duration knob for number-type video params currently lists only 5s / 10s (`play.html` DURATIONS), so the canvas chip may snap to 5s and quote ~$0.36; do not raise the JSON pin to 5 to match that knob. Headless / runner send 2s. Working nodes are text Still brief → Muse `meta/muse-image/text-to-image` @ 16:9 → ivideo (motion brief wired to prompt). Comments do not count.

This is still → Wan 3.0 motion + synced audio, not frame wakes up (`grok-imagine-still`: Muse + Grok Imagine Video 1.5 rooftop-ledge pulse, 4s / 480p) and not Animate a product still (`photo-to-video`: Muse + MiniMax H3 Spicy locked-camera asphalt rain, 5s / 480p / $0.20). Card title **still breathes with sound**. First-click is Volt charcoal/cyan radio on a rain-slick steel railing above a neon alley — magenta rim left, cyan puddle bounce, locked-cam breath, faint alley hum. The grok-imagine-still and photo-to-video graphs were not edited. Crystal upscale, H3 Max Multi Angle, Omni Flash turntable, InfiniteTalk, and the other concurrent Keep graphs stay on their own cards.

README share-link round-trip is the repo test for this addition. No paid Run and no paid NanoGPT spend; `graphs/wan-still-audio.NOTE.txt` records that cover QC is pending — no fake paid thumb. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this thirty-third graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`. Distinct from `wan-motion-drive` (still + driver clip, Animate 2 motion transfer) — do not retarget this graph.

## Wan 2.2 Animate 2 motion transfer — 2026-09-09

Public video catalog IDs (no paid generation this pass):

- `wan-22-animate-2` — Wan 2.2 Animate 2. Next-generation character motion transfer with strong identity preservation and prompt-controlled backgrounds. Requires a reference image and driver video; supports 480p/720p up to 120s. Live `video-models` id is `wan-22-animate-2` (no prefix). Image+video→video; tags wan / animate / motion-control / image-to-video / video-to-video. Pins `resolution=480p` (catalog default). Pricing 2026-09-09: $0.04/s at 480p, $0.08/s at 720p. Short driver ~2–3s ≈ $0.08–0.12. Optional `motion_prompt` (text) and `seed`. No duration additionalParam — output length follows the driver.
- `wan-22-animate` — Wan 2.2 Animate (v1). $0.05/s at 480p. Not this pin.
- `alibaba/wan-3.0/image-to-video` — Wan 3.0 Image-to-Video. Used by `wan-still-audio` only. Not in this graph.
- `pruna-ai/p-video/animate` — P-Video Animate. Not this job. Do not swap unless the Wan id is missing.
- `pruna-ai/p-video/edit` — P-Video Edit. Used by `p-video-rewrite` only. Not in this graph.
- `infinitetalk` — InfiniteTalk. Used by `infinitetalk-radio-take` only. Not in this graph.
- Do not pin Flare, Fable, Seedance, or `openai/gpt-4o-mini` for this job.

`wan-motion-drive` pins `wan-22-animate-2` at 480p. Working nodes are upload Character/product still → vupload Driver motion → text Motion prompt → vedit Driven clip (comments do not count). Still is wired to vedit `image` and `ref1` (runner collects `ref1..` as reference images; `image` is the visible image-ref socket). Driver is wired to `video`. Motion prompt is wired to `prompt`. No LLM. No Muse still.

This is motion transfer from an uploaded still + driver clip, not prompt-i2v (`wan-still-audio` / Wan 3.0, `grok-imagine-still`, `photo-to-video`), not InfiniteTalk / talking-avatar lipsync, not H3 Max multi-angle, and not P-Video rewrite. Card title **drive the still with motion**. First-click still is a Volt charcoal/cyan radio or courier; driver is a simple hand-wave / turn. Other Keep graphs were not edited. Draft test-pins (`deslop`), Flare/Fable showpieces, and custom-endpoint stay as they are.

README share-link round-trip is the repo test for this addition. No paid Run and no paid NanoGPT spend; `graphs/wan-motion-drive.NOTE.txt` records that cover QC is pending — no fake paid thumb. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this thirty-sixth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## Mirelo SFX1.6 video→foley — 2026-09-09

Public video catalog IDs (no paid generation this pass):

- `mirelo-ai/sfx1.6/video-to-video` — Mirelo SFX1.6 Video to Audio. Generate synchronized sound effects for an uploaded video and return the clip with a new audio track. Live `video-models` id keeps the `mirelo-ai/sfx1.6/` path. Tags: video-to-video / audio-for-video / sound-effects. Architecture `text+video->video`. Pricing $0.01/s; supported source durations 1–60s. Rechecked live 2026-09-10: ~4s ≈ $0.04. Marketing `/api/models` additionalParam is `seed` only (default -1) — not pinned. Catalog `nsfw: true` is a capability flag; first-click story is SFW.
- `mirelo-ai/sfx1.6/text-to-audio` — Mirelo SFX1.6 Text to Audio. Text→SFX sibling. Not this job.
- `mirelo-ai/sfx1.6/extend-audio` / `mirelo-ai/sfx1.6/inpaint-audio` — audio-only siblings. Not this job.
- `elevenlabs/sound-effects/v2` — ElevenLabs Sound Effects V2. Used by Night-ride SFX / `night-ride-sfx` only. Not in this graph.
- `xai-tts` — SpaceXAI TTS. Used by Night-ride radio VO / `night-ride-radio-vo` only. Not in this graph.
- `clarity-ai/crystal-video-upscaler` — Clarity Crystal. Used by `crystal-video-upscale` only. Not in this graph.
- `pruna-ai/p-video/edit` — P-Video Edit. Used by `p-video-rewrite` only. Not in this graph.
- `infinitetalk` — InfiniteTalk. Used by `infinitetalk-radio-take` only. Not in this graph.
- Do not pin Flare, Fable, or `openai/gpt-4o-mini` for this job.

`mirelo-video-foley` pins `mirelo-ai/sfx1.6/video-to-video` on a vedit node. Working nodes are vupload Product clip → vedit Foleyed clip (comments do not count). Catalog includes a text modality; first-click does not advertise a text SFX brief — the uploaded clip drives the foley (same empty-prompt vedit pattern as Crystal). No LLM. No TTS. No text SFX model. No local resize.

This is video→foley on an uploaded take, not text→SFX (`night-ride-sfx` / `elevenlabs/sound-effects/v2`), not Night-ride radio VO (`night-ride-radio-vo` / `xai-tts`), not Crystal upscale, not P-Video rewrite, not Sing, not InfiniteTalk, and not an i2v Keep (`photo-to-video`, `grok-imagine-still`, `wan-still-audio`, `h3-max-multi-angle`, Omni Flash). Card title **foley the take**; card desc **upload a clip — Mirelo writes matching SFX onto it**. First-click upload story is a Volt night-ride alley take. Other Keep graphs were not edited. Draft test-pins (`deslop`), Flare/Fable showpieces, and custom-endpoint stay as they are.

README share-link round-trip is the repo test for this addition. No committed Volt clip and no paid foley QC; `graphs/mirelo-video-foley.NOTE.txt` records that cover QC is pending (vupload placeholder only). Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this thirty-fourth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## Pixelcut video background removal — 2026-09-09

Public video catalog IDs (no paid generation this pass):

- `pixelcut/video-background-removal` — Pixelcut Video Background Remover. Remove video backgrounds with frame-by-frame AI segmentation and temporally consistent edges. Supports transparent output, preset solid backgrounds, custom RGB backgrounds, and common video formats. Live `video-models` id keeps the `pixelcut/` prefix. Video→video only; tags background-removal / video-to-video / utility / pixelcut / fal. `supported_parameters.background`: transparent (catalog default) / black / white / green / blue / magenta / custom. `background_color` hex when custom (default `#000000`). `output_format` auto / webm_vp9 / mp4_h264 / … — not pinned. Pricing $0.022 per 30 frames (~$0.044 for ~2s at 30 fps). Rechecked live 2026-09-10.
- `birefnet/v2` — BiRefNet V2. Used by Product cutout / `product-cutout` only. Not in this graph.
- `sam3-image` — SAM 3. Used by `sam3-isolate` only. Not in this graph.
- `background-remover` — still Background Remover (image catalog). Not this job.
- `clarity-ai/crystal-video-upscaler` — Clarity Crystal Video Upscaler. Used by `crystal-video-upscale` only. Not in this graph.
- `pruna-ai/p-video/edit` — P-Video Edit. Used by `p-video-rewrite` only. Not in this graph.

`pixelcut-video-cutout` pins `pixelcut/video-background-removal` at `background=black` in vedit `modelOpts`. Working nodes are vupload Product clip → vedit Cutout clip (comments do not count). No prompt — catalog `input_modalities` are video-only. No LLM. No still cutout. No Crystal / P-Video models. Solid black is the preview-friendly first-click; catalog default `transparent` is a later knob.

This is video background knock-out of an uploaded clip, not still BiRefNet cutout (`product-cutout` / `birefnet/v2`), not SAM 3 text-selected isolate (`sam3-isolate`), not Crystal target-megapixel upscale (`crystal-video-upscale`), not P-Video rewrite (`p-video-rewrite`), and not an i2v / orbit / turntable motion pin. Card title **knock the alley off the clip**. First-click upload story is a Volt charcoal/cyan radio night-ride take. Other Keep graphs were not retargeted. Draft test-pins (`deslop`), Flare/Fable showpieces, and custom-endpoint stay as they are. Do not pin `openai/gpt-4o-mini`.

README share-link round-trip is the repo test for this addition. No committed Volt clip and no paid cutout QC; `graphs/pixelcut-video-cutout.NOTE.txt` records that cover QC is pending (vupload placeholder only). Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this thirty-fifth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

## FLUX Virtual Try-On — Wear the night kit — 2026-09-10

Public image catalog IDs (no paid generation this pass):

- `flux-pro/v1/vto` — FLUX Virtual Try-On. Virtual try-on model that applies a garment reference image to a person image using styling instructions. Live `image-models` id is `flux-pro/v1/vto` (rechecked 2026-09-10; id did not move). Modality text+image→image; tags image-edit / virtual-try-on. Size `auto` only at catalog **$0.0575**/image. `nsfw: false`. Max 1 output image (`max_output_images` / `fixed_image_count`: 1). Exactly two input images: person first (max 2MP), garment second (max 1MP). Provider note is on `supported_parameters.input_image_constraints.provider`.
- `minimax-h3/image-edit` — MiniMax H3 Image Edit. Used by Night-ride identity restyle / `h3-identity-restyle` only. Not this job — identity-preserving scene/outfit restyle, not dedicated garment VTO.
- `meta/muse-image/edit` — Muse Edit. Used by Clean product photo / `edit-a-photo` and Product in a setting / `combine-images` only. Not this job.
- `birefnet/v2` — BiRefNet V2. Used by Product cutout / `product-cutout` only. Not in this graph.
- `sam3-image` — SAM 3. Used by `sam3-isolate` only. Not in this graph.
- Do not pin Ideogram transparent / Instant, Recraft vector, MAI pack type, or SenseNova infographic for this job.

`virtual-try-on` pins `flux-pro/v1/vto` at `auto`. Working nodes are upload Person still → upload Garment still → text Style brief → edit Try-on (comments do not count). Person is wired to edit `image` (must be first / person). Garment is wired to edit `image2` (must be second / garment). Style brief is wired to `prompt`. Empty default prompt on the edit node. No local resize. No LLM. No video.

nanoodle-js documents `IMG_INPUT_ROLES` for `flux-pro/v1/vto` as `[person, garment]` on ports `image` / `image2`. The runner refuses an unwired slot (reading raw port keys, not compacted collectPorts) so a garment-only wire cannot silently promote into the person role. Reversed order is charged and drifts identity toward the garment photo.

This is dedicated virtual garment try-on, not two-reference product placement (`combine-images` / Muse Edit), not catalog cleanup (`edit-a-photo` / Muse Edit), and not identity-preserving scene/outfit restyle (`h3-identity-restyle` / `minimax-h3/image-edit`). Card title **Wear the night kit**; desc **person + garment — FLUX tries the drop on**. First-click Style brief applies the garment from image 2 onto the person in image 1; preserve face/body identity and pose; preserve garment materials and the single cyan lightning-chevron; cool night-ride lighting (magenta rim + cyan bounce OK); no extra logos, text walls, props, or second people. Other Keep graphs were not edited. Custom-endpoint stays retired under `experiments/retired-gallery/`.

README share-link round-trip is the repo test for this addition. No committed person/garment stills and no paid NanoGPT spend; `graphs/virtual-try-on.NOTE.txt` records that cover QC is pending — no fake paid thumb. Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this thirty-seventh graph remain a follow-up. Do not treat catalog presence as output-quality evidence.

## Whisper Large V3 speech→text — 2026-09-09

Public audio catalog IDs (no paid generation this pass):

- `Whisper-Large-V3` — Whisper Large V3. OpenAI's state-of-the-art speech recognition model. Live `audio-models` id is `Whisper-Large-V3` (no prefix strip). `architecture.modality` audio→text; `capabilities.speech_to_text: true`; category `audio_stt`. Pricing $0.000495/min (≪$0.01 for a short VO). Language auto/blank (catalog auto language detection). Rechecked live 2026-09-10.
- `xai-tts` — SpaceXAI TTS. Used by Night-ride radio VO / `night-ride-radio-vo` only. Opposite arrow (text→speech). Not this job. Do not retarget that graph.
- `infinitetalk` — InfiniteTalk. Used by `infinitetalk-radio-take` only. Lipsync, not STT. Not in this graph.
- `elevenlabs/sound-effects/v2` — ElevenLabs Sound Effects V2. Used by Night-ride SFX / `night-ride-sfx` only. Not in this graph.
- `mirelo-ai/sfx1.6/video-to-video` — Mirelo SFX1.6. Used by foley the take / `mirelo-video-foley` only. Not in this graph.
- `gpt-4o-mini-transcribe` / `gpt-4o-mini-transcribe-2025-03-20` / `gpt-4o-mini-transcribe-2025-12-15` — OpenAI billable twins. Not this showpiece.
- `whisper-1` — OpenAI Whisper-1. Not this showpiece.
- `Elevenlabs-STT` / `Elevenlabs-Scribe-V2` — ElevenLabs Scribe. Diarization deferred. Not this job.
- Do not pin Demucs / Sonilo / stem, Flare/Fable, or `openai/gpt-4o-mini`. Use the `transcribe` node (not llm-with-audio).

`whisper-pull-words` pins `Whisper-Large-V3` on a `transcribe` node. Working nodes are aupload Night-ride VO → transcribe Transcript (comments do not count). Language left auto/blank. No LLM. No TTS. No music. No video.

This is speech→text on an uploaded VO, not Night-ride radio VO (`night-ride-radio-vo` / `xai-tts` text→speech), not InfiniteTalk / talking-avatar lipsync, and not Night-ride SFX / Mirelo foley (sound beds). Card title **pull the words off the take**; card desc **drop a night-ride VO — Whisper prints the transcript**. First-click upload story is a Volt charcoal/cyan radio night-channel take. Other Keep graphs were not retargeted. Draft test-pins (`deslop`), Flare/Fable showpieces, and custom-endpoint stay as they are.

README share-link round-trip is the repo test for this addition. No committed VO and no paid Whisper QC; `graphs/whisper-pull-words.NOTE.txt` records that cover QC is pending (aupload placeholder only — no fake transcript thumb). Sample gallery assets, nanoodle EXAMPLES sync and a live JS/Python materialize of this thirty-eighth graph remain a follow-up. Do not treat catalog presence as output-quality evidence. Custom-endpoint stays under `experiments/retired-gallery/`.

