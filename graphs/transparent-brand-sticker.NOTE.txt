Cover / sample-gallery QC is pending.

First-click Brand brief is a matte-charcoal field with ONE electric-cyan
lightning-bolt chevron sticker for Volt pocket night-ride radio / courier
brand. No letters on the mark. Flat sticker/overlay with clean alpha and
generous transparent margins — not a favicon mockup, not a product photo,
not a UI screen. Do not treat the graph as having a reviewed sticker.

This is text-to-image transparent-background generation
(`ideogram-v3-generate-transparent` at size `1:1`, catalog $0.06 at the
balanced default). Catalog also lists rendering_speed flash ~$0.03, but
the nanoodle image node does not forward modelOpts / additionalParams, so
this graph leaves the default.

Not Favicon concept (GLM Flash + Muse opaque square glyph). Not Product
cutout / BiRefNet (upload → knock out background). No upload. No LLM.

No paid NanoGPT / Ideogram generation was run for this addition.
