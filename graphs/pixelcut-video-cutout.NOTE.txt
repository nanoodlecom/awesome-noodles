Cover / sample-gallery QC is pending.

No Volt radio clip is committed in this repo. First-click is a video-upload
placeholder: matte-charcoal Volt pocket night-ride radio, charcoal/cyan
night-ride take. Do not treat the graph as having a reviewed cutout.

This is Pixelcut video background removal
(`pixelcut/video-background-removal` at background=black, $0.022/30 frames,
~$0.044 for ~2s). Solid black is the preview-friendly first-click pin;
catalog default `transparent` (plus white / green / blue / magenta / custom
hex) is a later knob — not this first-click. Not still BiRefNet cutout
(`product-cutout` / `birefnet/v2`), not SAM 3 isolate (`sam3-isolate`),
not still `background-remover`, not Crystal video upscale
(`crystal-video-upscale` / `clarity-ai/crystal-video-upscaler`), and not
P-Video rewrite (`p-video-rewrite` / `pruna-ai/p-video/edit`).

No paid NanoGPT generation was run for this addition.
