Cover / sample-gallery QC is pending.

No Volt radio still is committed in this repo. First-click is a
product-photo upload placeholder: a tight/cropped matte-charcoal Volt
pocket night-ride radio with one electric-cyan lightning-bolt chevron
and hard cyan rim that needs room around it for a wider hero. Do not
treat the graph as having a reviewed outpaint. Cover may reuse Volt
card art later on nanoodle.

This is canvas expand / outpaint only (`nano-banana-edit` at size
`auto`, catalog $0.039). Live catalog 2026-09-10: Nano Banana Edit
(Google / Gemini); text+image→image; resolutions `auto` only; catalog
`aspect_ratio` list includes `16:9` (16:9 intent lives in the Outpaint
brief — size stays `auto`). `max_input_images` 4; first-click uses ONE
upload. nsfw: false.

Not Clean product photo / Muse Edit (`edit-a-photo` / `meta/muse-image/edit`
— cleanup / backdrop replace). Not Night-ride identity restyle
(`h3-identity-restyle` / `minimax-h3/image-edit`). Not Remove packaging
text (`ideogram-v3-remove-text`). Not Product cutout / BiRefNet V2.
Not Text-selected isolate / SAM 3. Not P-Image Upscale
(`pruna-ai/p-image/upscale` — resolution only). Not SeedVR2 / Clarity
Crystal / other upscalers. Not FLUX VTO. Not Product in a setting /
combine-images (two-ref placement).

No paid NanoGPT generation was run for this addition.
