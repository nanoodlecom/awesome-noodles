Cover / sample-gallery QC is pending.

No courier / product-holder still is committed in this repo. First-click
is a mid-shot courier / product holder still that needs a cyan-rim
night-ride restyle (placeholder upload; cover QC pending). Do not treat
the graph as having a reviewed restyle.

This is identity-preserving scene/outfit/light restyle
(`minimax-h3/image-edit` at size `1k`, $0.03). Catalog allows ≤9 refs;
first-click uses one upload. nsfw: false.

Not Clean product photo / Muse Edit (cleanup / backdrop relight).
Not Cinematic character still / `minimax-h3/text-to-image` (T2I, no
upload identity). Not Product in a setting / combine-images
(two-reference placement). Not Product cutout / BiRefNet, Text-selected
isolate / SAM 3, Transparent brand sticker, Remove packaging text,
P-Image Upscale, InfiniteTalk, H3 Max Multi Angle, Flare-in-arena,
venice deslop, or character-sprites.

No paid NanoGPT generation was run for this addition.
